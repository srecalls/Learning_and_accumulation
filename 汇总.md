## 开发流程
1.拉取项目代码
2.根据团队node版本下载依赖项
3.

## 打点

```js
// 新建一个onetrack实例，区别于[./stat.js]，专门用于上报业务方打点需求

// https://git.n.xiaomi.com/fe/onetrack-js-sdk/-/blob/master/README-web.md#%E5%A4%9A%E5%AE%9E%E4%BE%8B
window.onetrack('BU.init', '31000000916');
window.onetrack('BU.set', {
  ref: 'admin_console',
  version_name: version,
});


const historyQueue = []; // 用于存放页面访问链路，页面曝光时通过trackPageView方法记录
let lastRoutePath = 'none'; // 用于保存用户浏览的最近的一个页面，页面曝光时通过trackPageView方法更新
const buTrack = (event, params) => {
  if (process.env.NODE_ENV === 'development') {
    // console.log(`BU.track: ${event}`, {
    //   miid: Cookie.get('userId'),
    //   developer_id: Cookie.get('developer_id'),
    //   developer_type: Cookie.get('developer_type'),
    //   server_time: new Date().getTime(),
    //   from_ref: historyQueue.length > 1 ? historyQueue[historyQueue.length - 2] : '',
    //   refs: historyQueue.join('-'),
    //   sub_ref: lastRoutePath,
    //   item_position: 0,
    //   ...params,
    // });
  } else {
    window.onetrack('BU.track', event, {
      miid: Cookie.get('userId'),
      developer_id: Cookie.get('developer_id'),
      developer_type: Cookie.get('developer_type'),
      server_time: new Date().getTime(),
      from_ref: historyQueue.length > 1 ? historyQueue[historyQueue.length - 2] : '',
      refs: historyQueue.join('-'),
      sub_ref: lastRoutePath,
      item_position: 0,
      ...params,
    });
  }
};
```

```js
// 页面曝光打点
let lastRouteTime = null; // 记录上一次页面访问的时间，用来计算页面停留时长
let lastExtra = {}; // 用来保存上一次页面曝光事件携带的参数（比如appId）
const trackPageView = (page, extra = {}) => {
	// 计算现在的时间
  const newRouteTime = new Date().getTime();
  // 
  const fromRef = historyQueue.length > 1 ? historyQueue[historyQueue.length - 2] : '';
  const data = {
    sub_ref: lastRoutePath,      // s
    server_time: lastRouteTime,
    duration: newRouteTime - lastRouteTime, // 持续时间
    refs: historyQueue.join('-'),   // 
    from_ref: fromRef,
  };
  if (!page && lastRouteTime) {
    // page参数不传，表示用户离开了当前路由且没有命中新的路由（比如关闭页面或切换tab），立即上报最近一次路由的view事件
    // lastRouteTime = null，下次用户再次回到页面时（切换TAB）视为新的view事件
    buTrack('view', {
      ...data,
      ...lastExtra,
    });
    lastRouteTime = null;
    return;
  }
  if (
    lastRouteTime
    && page === lastRoutePath
    && JSON.stringify(extra) === JSON.stringify(lastExtra)
  ) {
    // 该页面已经记录过了，不做任何修改
    return;
  }
  if (lastRouteTime) {
    // 由于要计算duration，因此每次路由切换时，上报上一个路由的view事件
    buTrack('view', {
      ...data,
      ...lastExtra,
    });
  }
  if (page) {
    historyQueue.push(page);
    lastRoutePath = page;
    lastExtra = extra;
  }
  lastRouteTime = newRouteTime;
};
window.addEventListener('visibilitychange', () => {
  if (lastRoutePath !== 'none') {
    trackPageView();
  }
});

```