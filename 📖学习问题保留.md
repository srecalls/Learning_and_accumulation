- [ ] 在5.React原理揭秘，2.推荐语法里面，为什么这种写法可以获得最新的state的值

系统防呆机制
代码质量
jira
Https有些可以，有些不可以
前端性能优化
[[1.什么是HTTPS协议]]
SOP
打桩测试






- PaaS
- SasS
- 知道就知道，不知道就不知道
- jira是bug的excel，项目管理工具
- 代码质量是项目正常交付是底线
- 码农与工程师
- 前端工程化（利用工具解决项目问题）
- 工程化工具
- 如何给用户更好的体验（性能优化，（重要的东西先呈现给用户（分包）），白屏是有东西冒出来的时间，骨架屏
- 金字塔
- 解决问题的方式（why what show）
- 如何解决eslint不遵守问题

- 沉下心来学东西，不要太功利
- 容器，是镜像，是模拟器
- 要看官方文档，先思考
- ![[img_v2_8b0aaab8-f6d0-4192-9b70-171a384fe44l.jpg]]
- 面试看的是亮点
- SOP是 标准流程
- npm ci是指根据lock来，直接npm i会更新lock
- 什么是mock
- 什么是动态规划
- 怎么样问问题
- gitlab 里面ci/cd（持续上传/持续部署）容器里面会帮你执行一些事情

为什么会产生依赖版本问题，跟node版本有关，跟npm包的版本有关，还是跟其他依赖这个包的包版本有关

依赖版本问题通常与多个因素相关，包括 Node.js 版本、npm 包的版本以及其他依赖于同一包的包的版本。

1. Node.js 版本：不同版本的 Node.js 可能对模块的依赖有不同的要求。某些包可能使用了较新的 JavaScript 语法或特性，这些特性可能在旧版本的 Node.js 中不被支持。因此，如果你的项目使用了较新的包，并且你的 Node.js 版本太旧，就可能导致依赖版本问题。

2. npm 包的版本：包的维护者可能会对其包进行更新，引入新的功能、修复漏洞或改变 API。如果你的项目依赖于一个包的旧版本，而其他依赖项使用了该包的新版本，就可能导致版本冲突和依赖版本问题。

3. 其他依赖于同一包的包版本：如果你的项目依赖于一个包 A（比如，版本 1.0.0），同时另一个包 B 也依赖于包 A，但要求的版本是 2.0.0，那么就会产生冲突。这是因为两个包依赖于同一包的不同版本，而这两个版本可能具有不兼容的更改或功能差异。

为了解决依赖版本问题，可以采取以下方法：

1. 更新 Node.js 版本：确保你的 Node.js 版本与你的项目依赖的包兼容。查看包的文档或 README 文件，了解其所需的最低 Node.js 版本，并相应地升级你的 Node.js。

2. 更新 npm 包：使用 npm 更新命令可以将依赖包升级到最新版本。然而，需要注意升级可能会导致其他依赖的不兼容性问题，所以在升级之前最好备份你的项目或做好测试。

3. 锁定依赖版本：使用 package.json 文件中的版本范围或精确版本来锁定依赖项的版本。这样做可以确保每个开发者或构建环境都使用相同的版本，减少版本冲突的可能性。

4. 使用工具管理依赖：使用工具如 Yarn 或 npm 的 package-lock.json 或 yarn.lock 文件可以更好地管理依赖版本。这些工具可以确保在不同环境中使用相同的版本，并帮助解决依赖冲突。

总结来说，依赖版本问题可能与多个因素相关，包括 Node.js 版本、npm 包的版本以及其他依赖于同一包的包的版本。通过适当的版本管理和工具，可以减少依赖版本问题的发生，并确保项目的稳定性和一致性。


灰度
白名单


nginx代理服务器，是做代理的，灰度发布就是nginx做白名单，将对应的资源分发给对应的人
上传cookie进过nginx验证进行过滤
SRE是运维
