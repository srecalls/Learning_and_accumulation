https
Http over ssl or tls
通信。       加密，
危害：
1. 数据容易被窃听
当使用公共网络时，可以通过网络嗅探工具捕获HTTP通信的数据包，
因为http是采用明文传输的
2. 数据容易被篡改
攻击者容易通过中间人攻击来篡改
3. 容易出现假冒网站
4. Seo容易受损
如果使用http而不是用https，搜索排名可能会受到影响
seo算法会优先考虑使用https的网址

tls（transport layer security）传输层安全协议
ssl （secure socket layers）安全套接字层协议
可以对数据进行加密验证
tls实际上是ssl的一个更新版本
他修复了早起ssl协议中的一些漏洞
![[字节跳动秋招-深圳-抖音前端一面.png]]



现在所有的浏览器都停止了对ssl的支持，而且如果用tls1.0或1.1，有些浏览器也会进行一些警告提示

https如何通过ssl证书加密网络传输

1. 对称加密过程
- 浏览器发送一个加密套件列表，在浏览器再生成一个随机数发送给服务器
加密套件列表：支持加密方法的一个列表
- 服务器选择一个加密套件也生成一个随机数，然后返回加密套件和随机数，最后浏览器和服务器互相确认一下，那么安全握手协商就算完成了
- ![[https.png]]

接下来传输数据就可以通过刚才的加密套件吧浏览器生成的随机数和浏览器生成的随机数混合在一起生成一个密钥，然后再利用密钥进行加密的传输

![[https-1.png]]
即是截获了数据，也无法正确解析到内容

![[https-2.png]]

但是握手的时候随机数和加密套件都是明文的，如果在握手阶段进行攻击获取报文，那么密钥也可以在攻击者方生成一个，以后传输的数据还是可以被破译，那么如何解决这个握手的阶段
使用非对称加密

2. 非对称加密
非对称加密的密钥有两把
这两把密钥无论用哪一把钥匙，另一把都可以对数据进行破解，但是他和对称加密不同的一点就是，如果我们用同一个密钥对数据进行加密，那这个密钥不能对这个数据进行解锁，
![[https-3.png]]




好处就是可以我们可以给别人公钥，，别人使用公钥对数据进行加密，把数据返回给我，只有自己的私钥可以对数据进行解密，这样即使攻击者中途拿到公钥，截获了数据也不开。
因此可以这样改造，

1. 浏览器发送加密套件列表，服务器收到加密套件后，选择一个加密套件，然后把这个加密套件和服务器的一个公钥一起发送给浏览器，浏览器和服务器再次互发消息进行一个确认，那么安全握手协商就算完成，以后浏览器再给服务器发送信息之前，都要用这个公钥对数据进行一个加密
2. ![[https-4.png]]


但这样仍然有一个问题
![[https-5.png]]

浏览器发送请求是用公钥进行加密，攻击者无法获得数据，但是服务端发送给浏览器是通过私钥来进行加密的，任何人都可以拥有公钥，所以服务端发送给客户端的数据，无法保证其安全性

第二点就是非对称加密的效率比较低

因此可以采取
![[https-6.png]]
这样就可以解决传输效率低和只有一方被加密的问题，这就叫做混合加密


安全握手的协商流程：
	1. 浏览器给服务端传输一个对称加密套件列表与非对称加密列表告诉服务端浏览器所支持的加密方式同时也带着一个随机数用来生成最后的加密密钥，服务端保存浏览器的随机数，选择相应的套件并且生成一个服务端的随机数，最后服务端把服务端生产的公钥，套件和随机数都传送给浏览器，注意服务端的私钥还是放在浏览器，谁也不发送，因为这个私钥是私有的，浏览器对公钥有一个保存，然后结合前两个随机数生成一个Premaster随机数，浏览器把这个随机数通过服务器公钥进行加密，然后发送给服务端确认，服务器再拿自己的私钥对加密后的Premaster随机数进行解析，得到这个随机数，接下来浏览器和服务端通过这三个随机数共同创造一个新的密钥，这样就可以拿着这个密钥对数据进行加密，在这种情况下密钥是不会被破解的，因为他在传输的过程中采用非对称加密，只有服务端的私钥可以解开它，以后可以通过这个密钥，对数据进行对称加密，从而进行安全的数据传输
![[https-8.png]]

因为我这个pre-master是无法得到的，从而导致生成的最后的密钥是无法得到的



这种情况仍然会出现一个问题，直接在浏览器和服务器之间切断了链接，然后伪装成服务端，而攻击者相对于服务端伪装成浏览器，我们以为自己与服务端已经建立完了混合加密的协商，但其实我们只是和攻击者的服务端建立了加密协商，攻击者可以拿着我们发送的数据在向真实的服务端建立混合加密然后代替我们与服务端进行沟通，这种攻击方式叫做中间人攻击
![[https-9.png]]
为了解决这个问题，还需要判断服务器是否为好的服务器，这时候我们需要找一个第三方来证明服务端是一个好的服务端，这个第三方就是CA
![[https-10.png]]
全称叫做Certificate Authority
中文名叫做证书颁发机构，那这个证书就叫做数字证书
![[https-12.png]]
这个CA颁布的证书里面除了能证明我们服务端的一个身份，还包含了服务端的一个公钥，
![[https-13.png]]
这样我们可以在混合加密的基础上让服务端通过证书来自证清白


流程如下：
首先浏览器会向服务端传输加密套件列表和非加密套件列表并生成一个随机数传给服务器，服务器保存浏览器传过来的随机数。，选择相应的套件，并生产一个服务器随机数，这时候就不是传输公钥，而是传输数字证书，浏览器收到证书，并且对证书进行验证，然后取证书里服务端的一个公钥进行保存，那后续的步骤其实就和混合加密一样了，只不过多了证书验证的部分，能在一定程度上确保服务端的安全性
![[https-14.png]]


![[https-15.png]]

证书是如何申请的

首先我们要给服务器生成一套公钥和私钥，给CA机构提供一个公钥还有公司的具体信息和站点信息一般还要交点钱，

![[https-16.png]]

CA会对信息进行审核，来确定你的公司是否合法，站点是否在这个公司名下

![[Pasted image 20231024204350.png]]
，如果审核通过的话会得到一个包含公钥组织信息，ca信息，ca签名有效期等消息等证书，另外这个证书上面的东西都是明文，


![[https-18.png]]
什么是ca签名，ca签名其实就是用来验证证书的有效性的，这个签名是通过哈希函数计算服务端的明文信息，得出的一个信息摘要，然后再用ca的私钥对信息摘要进行加密，加密之后就得到了数字签名，那么浏览器上我们的验证过程就是读取证书上的明文信息然后采用ca采用的哈希函数计算得到的信息摘要接下来利用浏览器内置的ca公钥把数字证书上的签名进行解密，得到第二份信息摘要，然后比较这两份摘要，如果一直就能证明这个证书的有效性
[[1.回流与重绘的概念及触发条件]]
[[http缓存（浏览器缓存）——强缓存、协商缓存]]
![[https-19.png]]

