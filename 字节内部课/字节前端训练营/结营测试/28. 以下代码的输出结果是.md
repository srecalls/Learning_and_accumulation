以下代码的输出结果是：
```js
for (let i = 0; i < 5; i++){
	setTimeout(function(){
		console.log(i);
	}, 100*i)
}
```


这段代码的输出结果将会是：

```
0
1
2
3
4
```

这是因为在使用 `let` 关键字定义变量时，会将变量绑定到当前的块级作用域中。在每次循环迭代中，都会创建一个新的变量 `i`，并将其绑定到回调函数中。因此，每个回调函数都捕获了自己的变量 `i`，并且这些变量是相互独立的。因此，每个回调函数都会输出自己捕获的变量 `i` 的值，输出结果是 0、1、2、3、4。