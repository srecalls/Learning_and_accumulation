难度：<font color ="lightgreen">简单</font>

给定两个数组，`arr1` 和 `arr2`，

- `arr2` 中的元素各不相同
- `arr2` 中的每个元素都出现在 `arr1` 中

对 `arr1` 中的元素进行排序，使 `arr1` 中项的相对顺序和 `arr2` 中的相对顺序相同。未在 `arr2` 中出现过的元素需要按照升序放在 `arr1` 的末尾。

**示例：**

	输入：arr1 = [2,3,1,3,2,4,6,7,9,2,19], arr2 = [2,1,4,3,9,6]
	输出：[2,2,2,1,4,3,3,9,6,7,19]

**提示：**

- `1 <= arr1.length, arr2.length <= 1000`
- `0 <= arr1[i], arr2[i] <= 1000`
- `arr2` 中的元素 `arr2[i]` 各不相同
- `arr2` 中的每个元素 `arr2[i]` 都出现在 `arr1` 中

注意：本题与主站 1122 题相同：[https://leetcode-cn.com/problems/relative-sort-array/](https://leetcode-cn.com/problems/relative-sort-array/)


## 我的提交
### version1
```
arr1 =[1,2,3,4,5,0]

arr2 =[4,1,3]

[4,1,3,0,2,5]
```

```js
/**
 * @param {number[]} arr1
 * @param {number[]} arr2
 * @return {number[]}
 */
var relativeSortArray = function(arr1, arr2) {
    arr1.sort((b, a) => {
        if (arr2.indexOf(b) !== -1 && arr2.indexOf(a) !== -1) {
            return arr2.indexOf(b) - arr2.indexOf(a)
        }
        if (arr2.indexOf(b) === -1 && arr2.indexOf(a) === -1) {
            return b - a
        }
    })
    return arr1
};
```

时间80ms击败 11.80%使用 JavaScript 的用户
内存40.08mb击败 81.37%使用 JavaScript 的用户

### version2
```js

```