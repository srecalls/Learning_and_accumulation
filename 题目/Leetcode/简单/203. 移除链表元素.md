给你一个链表的头节点 `head` 和一个整数 `val` ，请你删除链表中所有满足 `Node.val == val` 的节点，并返回 **新的头节点** 。

**示例 1：**
![[203. 移除链表元素.png]]

	输入：head = [1,2,6,3,4,5,6], val = 6
	输出：[1,2,3,4,5]

**示例 2：**

	输入：head = [], val = 1
	输出：[]

**示例 3：**
	
	输入：head = [7,7,7,7], val = 7
	输出：[]

**提示：**

- 列表中的节点数目在范围 `[0, 104]` 内
- `1 <= Node.val <= 50`
- `0 <= val <= 50`

## 我的提交
```js
/**
 * Definition for singly-linked list.
 * function ListNode(val, next) {
 *     this.val = (val===undefined ? 0 : val)
 *     this.next = (next===undefined ? null : next)
 * }
 */
/**
 * @param {ListNode} head
 * @param {number} val
 * @return {ListNode}
 */
var removeElements = function(head, val) {
    if (!head) return null
    while (head && head.val === val) {
        head = head.next
    }
    let ret = head
    while(ret && ret.next) {
        if (ret.next.val === val) {
            ret.next = ret.next.next
        } else {
            ret = ret.next
        }
    }
    return head
};
```

时间76ms击败 71.99%使用 JavaScript 的用户
内存43.98mb击败 95.89%使用 JavaScript 的用户

## 官方答案
![[203. 移除链表元素-1.png]]
```Js
var removeElements = function(head, val) {
    const dummyHead = new ListNode(0, head);
    let temp = dummyHead;
    while (temp.next !== null) {
        if (temp.next.val == val) {
            temp.next = temp.next.next;
        } else {
            temp = temp.next;
        }
    }
    return dummyHead.next;
};
```


时间76ms击败 71.99%使用 JavaScript 的用户

内存44.40mb击败 65.08%使用 JavaScript 的用户

![[203. 移除链表元素-2.png]]