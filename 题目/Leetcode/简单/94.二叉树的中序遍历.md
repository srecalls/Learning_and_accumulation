难度：<font color ="lightgreen">简单</font>
给定一个二叉树的根节点 root ，返回 它的 中序 遍历 。

 

示例 1：

![[94.二叉树的中序遍历.png]]

	输入：root = [1,null,2,3]
	输出：[1,3,2]

示例 2：

	输入：root = []
	输出：[]

示例 3：

	输入：root = [1]
	输出：[1]


提示：

	树中节点数目在范围 [0, 100] 内
	-100 <= Node.val <= 100
	 

进阶: 递归算法很简单，你可以通过迭代算法完成吗？

来源：力扣（LeetCode）
链接：https://leetcode.cn/problems/binary-tree-inorder-traversal

## 我的提交
### 递归
```js
/**
 * Definition for a binary tree node.
 * function TreeNode(val, left, right) {
 *     this.val = (val===undefined ? 0 : val)
 *     this.left = (left===undefined ? null : left)
 *     this.right = (right===undefined ? null : right)
 * }
 */
/**
 * @param {TreeNode} root
 * @return {number[]}
 */

var inorderNode = function(root, res) {
    if (root.left) inorderNode(root.left, res)
    res.push(root.val)
    if (root.right) inorderNode(root.right, res)
}

var inorderTraversal = function(root) {
    if (!root) return []
    let res = []
    inorderNode(root, res)
    return res
};
```

执行用时：68 ms, 在所有 JavaScript 提交中击败了24.96%的用户
内存消耗：40.9 MB, 在所有 JavaScript 提交中击败了95.69%的用户


### 迭代
```js
/**
 * Definition for a binary tree node.
 * function TreeNode(val, left, right) {
 *     this.val = (val===undefined ? 0 : val)
 *     this.left = (left===undefined ? null : left)
 *     this.right = (right===undefined ? null : right)
 * }
 */
/**
 * @param {TreeNode} root
 * @return {number[]}
 */

var inorderTraversal = function(root) {
    let res = []
    let stack = []
    while(root || stack.length) {
        while(root) {
            stack.push(root)
            root = root.left
        }
        root = stack.pop()
        res.push(root.val)
        root = root.right
    }
    return res
};
```
执行用时：56 ms, 在所有 JavaScript 提交中击败了88.20%的用户
内存消耗：41.1 MB, 在所有 JavaScript 提交中击败了71.57%的用户