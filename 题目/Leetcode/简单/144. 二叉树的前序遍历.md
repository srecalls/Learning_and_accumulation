难度：<font color ="lightgreen">简单</font>
给你二叉树的根节点 `root` ，返回它节点值的 **前序** 遍历。
**示例 1：**
![[144. 二叉树的前序遍历.png]]
	
	输入：root = [1,null,2,3]
	输出：[1,2,3]

示例 2：

	输入：root = []
	输出：[]

示例 3：

	输入：root = [1]
	输出：[1]

示例 4：
![[144. 二叉树的前序遍历-1.png]]

	输入：root = [1,2]
	输出：[1,2]

示例 5
![[144. 二叉树的前序遍历-2.png]]

	输入：root = [1,null,2]
	输出：[1,2]

**提示：**

- 树中节点数目在范围 `[0, 100]` 内
- `-100 <= Node.val <= 100`

**进阶**：递归算法很简单，你可以通过迭代算法完成吗？


来源：力扣（LeetCode）
链接：https://leetcode.cn/problems/binary-tree-preorder-traversal

## 我的提交

### 迭代
```js
/**
 * Definition for a binary tree node.
 * function TreeNode(val, left, right) {
 *     this.val = (val===undefined ? 0 : val)
 *     this.left = (left===undefined ? null : left)
 *     this.right = (right===undefined ? null : right)
 * }
 */
/**
 * @param {TreeNode} root
 * @return {number[]}
 */

var preorderTraversal = function(root) {
    if (!root) return []
    let res = []
    let stack = [root]
    while(stack.length) {
        let cur = stack.pop()
        res.push(cur.val)
        if (cur.right) stack.push(cur.right)
        if (cur.left) stack.push(cur.left)
    }
    return res
};
```
执行用时：64 ms, 在所有 JavaScript 提交中击败了46.28%的用户
内存消耗：41.2 MB, 在所有 JavaScript 提交中击败了46.87%的用户

### 递归
```js
/**
 * Definition for a binary tree node.
 * function TreeNode(val, left, right) {
 *     this.val = (val===undefined ? 0 : val)
 *     this.left = (left===undefined ? null : left)
 *     this.right = (right===undefined ? null : right)
 * }
 */
/**
 * @param {TreeNode} root
 * @return {number[]}
 */

 var preorderNode = function(root, res) {
     res.push(root.val)
     if (root.left) preorderNode(root.left, res)
     if (root.right) preorderNode(root.right, res)
 }

var preorderTraversal = function(root) {
    if (!root) return []
    let res = []
    preorderNode(root, res)
    return res
};
```
执行用时：60 ms, 在所有 JavaScript 提交中击败了69.63%的用户
内存消耗：41.1 MB, 在所有 JavaScript 提交中击败了54.67%的用户