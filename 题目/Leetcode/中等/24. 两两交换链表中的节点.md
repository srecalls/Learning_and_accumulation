难度：<font color ="orange">中等</font>
给你一个链表，两两交换其中相邻的节点，并返回交换后链表的头节点。你必须在不修改节点内部的值的情况下完成本题（即，只能进行节点交换）。

**示例 1：**
![[24. 两两交换链表中的节点.png]]

	输入：head = [1,2,3,4]
	输出：[2,1,4,3]

**示例 2：**
	
	输入：head = []
	输出：[]

**示例 3：**
	
	输入：head = [1]
	输出：[1]

**提示：**

- 链表中节点的数目在范围 `[0, 100]` 内
- `0 <= Node.val <= 100`

## 官方答案
![[24. 两两交换链表中的节点-1.png]]
![[24. 两两交换链表中的节点-2.png]]
![[24. 两两交换链表中的节点-4.png]]
![[24. 两两交换链表中的节点-3.png]]
```js
/**
 * Definition for singly-linked list.
 * function ListNode(val, next) {
 *     this.val = (val===undefined ? 0 : val)
 *     this.next = (next===undefined ? null : next)
 * }
 */
/**
 * @param {ListNode} head
 * @return {ListNode}
 */
var swapPairs = function(head) {
    let dummyHead = new ListNode(0, head)
    let temp = dummyHead
    while (temp.next && temp.next.next) {
        let node1 = temp.next
        let node2 = temp.next.next
        temp.next = node2
        node1.next = node2.next
        node2.next = node1
        temp = node1
    }
    return dummyHead.next
};
```

时间56ms击败 86.03%使用 JavaScript 的用户
内存40.20mb击败 85.79%使用 JavaScript 的用户