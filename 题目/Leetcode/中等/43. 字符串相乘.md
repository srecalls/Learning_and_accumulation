给定两个以字符串形式表示的非负整数 `num1` 和 `num2`，返回 `num1` 和 `num2` 的乘积，它们的乘积也表示为字符串形式。

注意：不能使用任何内置的 BigInteger 库或直接将输入转换为整数。

示例 1:

	输入: num1 = "2", num2 = "3"
	输出: "6"

示例 2:

	输入: num1 = "123", num2 = "456"
	输出: "56088"
 

提示：

	1 <= num1.length, num2.length <= 200
	num1 和 num2 只能由数字组成。
	num1 和 num2 都不包含任何前导零，除了数字0本身。

## 我的提交
```js
/**
 * @param {string} num1
 * @param {string} num2
 * @return {string}
 */
var multiply = function(num1, num2) {
    let arrNum1 = num1.split('')
    let arrNum2 = num2.split('')
    let len1 = arrNum1.length
    let len2 = arrNum2.length
    let pos = []
    for (let j = len2 - 1; j >= 0; j--) {
        for (let i = len1 - 1; i >=0; i--) {
            let index1 = i + j
            let index2 = i + j + 1
            let mul = arrNum2[j] * arrNum1[i] + (pos[index2] || 0)
            pos[index1] = Math.floor(mul / 10) + (pos[index1] || 0)
            pos[index2] = mul % 10
        }
    }
    let res = pos.join('').replace(/^0+/, '')
    return res || '0'
};
```

执行结果：

	通过
	执行用时：76 ms, 在所有 JavaScript 提交中击败了57.86%的用户
	内存消耗：42.8 MB, 在所有 JavaScript 提交中击败了53.21%的用户
	通过测试用例：311 / 311