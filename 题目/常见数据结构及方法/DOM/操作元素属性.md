## 操作元素表单属性
获取：DOM对象.用户名
设置：DOM对象.属性名 = 新值

```js
表单.value = '用户名'
表单.type = 'password'
```

表单属性中添加就有效果,移除就没有效果，一律使用布尔值表示 如果为true 代表添加了该属性 如果是false 代表移除了该属性
比如: disabled、checked、selected (下拉框才有)

```js

```
### input类型
