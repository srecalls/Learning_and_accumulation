在 JavaScript 中，可以使用欧几里得算法（也称为辗转相除法）来计算两个数的最大公约数（GCD）。欧几里得算法的基本思路是，用较小的数除以较大的数得到余数，然后用余数去除较小的数，再得到余数，如此循环，直到余数为0，此时较大的数就是最大公约数。

以下是使用欧几里得算法实现求两个数的最大公约数的 JavaScript 代码：

```javascript
function gcd(a, b) {
  if (b === 0) {
    return a;
  } else {
    return gcd(b, a % b);
  }
}
```
24 36
36 24
24 12
12 0

在上面的代码中，我们使用递归的方式实现了欧几里得算法。如果`b`为0，则`a`就是最大公约数。否则，我们使用`a`除以`b`得到余数，然后将`b`和余数作为输入递归调用`gcd`函数，直到`b`为0。这样可以保证`gcd`函数的输出为两个数的最大公约数。

例如，如果我们要求24和36的最大公约数，可以调用`gcd(24, 36)`，它的输出为12。