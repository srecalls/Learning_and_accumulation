## set 、map的共同点和不同点

A：

set 是一种叫做集合的数据结构,Map是一种叫做字典的数据结构。
Set 和 Map 主要的应用场景在于 数据重组 和 数据储存。
共同点：可以遍历，存储唯一的值.

不同点：set是以 [value, value]的形式储存元素，map是以 [key, value] 的形式储存

1.set类似数组；set里面的成员不可重复，是唯一；set只有键值

2.map类似集合；map有键值对。
