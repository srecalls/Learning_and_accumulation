**作用：对象的属性或方法可选**
注意：区别于**函数可选参数**，这个是**对象**的可选

```ts
function myAxios(config: { url: string; method?: string}) {
	console.log(config)
}
```
# 可选属性 ？
![[Pasted image 20230327011804.png]]
![[Pasted image 20230327011832.png]]