**作用：元组类型可以确切地标记处有多少个元素，以及每个元素的类型。**（可以标记有多少个元素以及类型，比数组严谨）
```ts
let position: number[] = [30, 40] // Array
let position: [number, number] = [30, 40] // Turple
let position: [number, String] = [30, '40'] // Turple
```
# 元组
![[Pasted image 20230327012927.png]]
![[Pasted image 20230327012946.png]]