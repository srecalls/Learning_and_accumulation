
```ts
ts采用的是结构化类型系统，类型检查关注的是值所具有的形状
```

```ts
class Point { x: number; y: number }
class Point2D { x: number; y: number }

const p: Point = new Point2D() // 结构相同，兼容（都有x、y，且属性类型也同）

java、c#用的是标明类型系统。他们是不同的类，类型无法兼容
```
# 类型兼容性说明
![[Pasted image 20230327164422.png]]
![[Pasted image 20230327164451.png]]
