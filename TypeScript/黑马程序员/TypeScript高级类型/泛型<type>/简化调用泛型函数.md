# 简化调用泛型函数
**作用：使用这种简化的方式调用泛型函数，使代码更短，更易于阅读。**

```ts
function id<Type>(value: Type): Type { return value }
let num = id<number>(10) // 可以简化为
let num = id(10) 利用类型参数推断机制
当无法判读时候就需要显式的传入类型参数
```

![[Pasted image 20230328013514.png]]


![[Pasted image 20230328013622.png]]