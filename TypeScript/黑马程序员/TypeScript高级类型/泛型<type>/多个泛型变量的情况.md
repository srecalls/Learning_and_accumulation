# 多个泛型变量的情况
```ts
泛型的类型变量可以有多个，岂可类型变量之间可以约束
function getProp<Type, Key extends keyof Type>(obj: Type, key: Key) {
	return obj[key]
}
let person = { name: 'jack', age: 18 }
getProp(person, 'name')
```

![[Pasted image 20230328022046.png]]
![[Pasted image 20230328022431.png]]