
![[2.2.3 外部扩展（externals）.png]]

_externals翻译_：外表；外觀；外在形式

比如jQuery包有点大，本地安装会增加打包的体积，不打算安装，又不想直接放在window对象上面，想利用类似本地安装好这种方式去载入着这个包
![[2.2.3 外部扩展（externals）-1.png]]


这里会报错，因为没有本地安装
需要新的 配置externals，可以定义外部的第三方的包

注意key的名字要和app.js里引用的包名相同
value就是window上暴露的对象
比如将来可以利用cdn的方式加载一个模块，就可以用window上暴露的对象
比如通过script标签加载一个cdn的链接
jQuery会在window上暴露一个 jQuery
![[2.2.3 外部扩展（externals）-2.png]]
然后执行打包，会发现main.js是4.31kb，说明没有打包jQuery
但这是在浏览器上不能正常进行的

![[2.2.3 外部扩展（externals）-3.png]]
必须全局暴露一个链接
如何暴露呢，在index.html里加入一个script标签
![[2.2.3 外部扩展（externals）-5.png]]

![[2.2.3 外部扩展（externals）-4.png]]
![[2.2.3 外部扩展（externals）-6.png]]
这次就能正常运行了


每次都往模版页面（index.html）加上cdn不是很方便，能不能用webpack自动打包的方法加载这个script标签呢？

如果可以，甚至都不用创建index.html和修改webpack的配置

只需要把externals里的'jQuery'改成一个数组
![[2.2.3 外部扩展（externals）-7.png]]
第一个参数是cdn的字符串
第二个是暴露的对象
然后会报错，报错原因是没有告诉webpack这个链接是以什么方式引入
![[2.2.3 外部扩展（externals）-8.png]]
设置externalsType

![[2.2.3 外部扩展（externals）-9.png]]
正常工作

