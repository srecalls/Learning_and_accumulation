使用HtmlWebpackPlugin解决需要手工修改index.html里面去加载那个bundle.js文件的路径
，才能够在浏览器里正常显示的问题
![[1.3.2 使用HtmlWebpackPlugin.png]]

![[1.3.2 使用HtmlWebpackPlugin-1.png]]
这里HtmlWebpackPlugin开头大写，说明是一个类或者构造函数，所以需要实例化他一下。
此时执行webpack

![[1.3.2 使用HtmlWebpackPlugin-2.png]]
dist文件夹里除了budle.js还会多一个index.html

在这个html里会在head标签里面多一个script标签，他就是这个插件帮助我们生成的
此时生成index.html与外层的index.html是没有关系的，并不是基于外层的生成内层的，要让他们有联系，需要我们修改一下配置

![[1.3.2 使用HtmlWebpackPlugin-3.png]]

在构造函数内添加配置项
template表示以什么为模版进行生产
filename表示生成的文件名
inject表示生成的script标签在哪个位置

但此时我此前生成的index.html还在，我能不能在生成新的打包文件的时候，把上次遗留的东西清理掉呢？
