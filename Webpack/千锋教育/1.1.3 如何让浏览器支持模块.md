## 前期
是利用browsefy和requirejs这样的打包工具来编写能够在我们浏览器中运行 commonjs 的 模块的代码
![[1.1.3 如何让浏览器支持模块.png]]
以requirejs为例
requirejs提供了一个define方法，里面包括两个参数，第一个参数是一个数组，包含该模块依赖的模块，第二个是一个回调函数，返回暴露的对象或方法

如何引用：
![[1.1.3 如何让浏览器支持模块-1.png]]
requirejs提供一个data-main=“”字段，其中填写入口js文件

requirejs提供了一个require方法，第一个参数是数组，里面是依赖的 模块，这里的路径是以引入的html为准，第二个参数为回调函数，在入口js文件中

![[1.1.3 如何让浏览器支持模块-2.png]]
![[1.1.3 如何让浏览器支持模块-3.png]]


## 目前：
ECMAScript
ECMAScript module 来去定义和导出一个模块
![[1.1.3 如何让浏览器支持模块-4.png]]
ESM利用export defalut导出一个模块


![[1.1.3 如何让浏览器支持模块-5.png]]
然后在html中导入并且使用，此时会报一个错
![[1.1.3 如何让浏览器支持模块-6.png]]
我们不能够在一个模块的外部去使用 import 这个关键字

这是因为缺少一个type关键字
![[1.1.3 如何让浏览器支持模块-7.png]]
然后此时在浏览器输出
![[1.1.3 如何让浏览器支持模块-8.png]]
通过了一个null的域，其实事实上他违背了一个叫做 跨域的 安全策略，告诉我们通过file协议去访问我们的文件是有问题的，这是需要在本地临时搭建一个http server这样的环境

npx可以实现当一个模块在本地不存在时可以从互联网上安装一个模块

npx http-server启动服务器
![[1.1.3 如何让浏览器支持模块-9.png]]
进入8080端口，点击html进入，此时会进行显示
![[1.1.3 如何让浏览器支持模块-10.png]]

但是目前浏览器对ECMA Script中的ESM支持还不完整
那是否有一种好的方式，不仅可以让我们编写模块，而且还可以支持任何的模块格式
**Webpack**

既可以打包javascript应用程序，并且同时支持 es 的模块化以及 commonjs，还可以支持很多静态资源打包，比如图片，字体文件，样式文件

## webpack与竞品
![[1.1.3 如何让浏览器支持模块-12.png]]

parcel号称零配置，用户一般无需做其他的配置开箱即用
rollupjs 用标准化的格式来编写代码，比如es6。roullup通过减少无用的代码，来尽可能减少包的体积
，一般只能用来打包js
如果想要构建一个简单的应用，并且想让他快速的运行起来，那可以使用parcel
如果想要构建一个类库，以需要导入很少第三方库，可以使用rollup

webpack与vite有各自的使用场景

## 小结
![[1.1.3 如何让浏览器支持模块-13.png]]
