在网页开发中，**文档标准流（Normal Flow）**，也常被称为**常规流**，是浏览器在渲染 HTML 文档时默认的排版布局规则。

如果把网页比作一个“容器”，标准流就是你往容器里放东西时，物体受重力影响自然下落、堆叠的过程。理解了标准流，你才真正理解了 CSS 布局的基础。

---

## 1. 核心定义

标准流是指在没有任何 CSS 样式干预（如 `float`、`position: absolute` 或 `flex`）的情况下，浏览器根据 HTML 元素的出现顺序，**从左到右、从上到下**依次排列元素的过程。

### 标准流的两个“基本动作”：

1. **水平排列**：如果一行放得下，就接着放。
    
2. **垂直换行**：如果一行放不下，或者遇到了必须独占一行的元素，就另起一行。
    

---

## 2. 元素在标准流中的两种角色

在标准流中，HTML 元素根据其表现特征，主要分为两大类：**块级元素**和**行内元素**。

### A. 块级元素 (Block-level Elements)

块级元素就像是建筑里的“砖块”。

- **排列特征**：总是从新行开始，并垂直地向下堆叠。
    
- **宽度表现**：默认宽度是其父容器宽度的 **100%**。
    
- **盒子模型**：可以设置 `width`、`height`、`padding` 和 `margin`。
    
- **常见元素**：`<div>`、`<h1>-<h6>`、`<p>`、`<ul>`、`<li>`、`<section>` 等。
    

### B. 行内元素 (Inline Elements)

行内元素就像是“文本流”中的文字。

- **排列特征**：在同一行内从左向右排列，直到该行空间用完才会换行。
    
- **宽度表现**：宽度由其内容（如文字或图片）支撑，**不可手动设置宽高**。
    
- **盒子模型**：可以设置水平方向的 `padding` 和 `margin`，但垂直方向的设置不会改变行高，也不会影响周围元素的布局。
    
- **常见元素**：`<span>`、`<a>`、`<strong>`、`<em>` 等。
    

> **注**：还有一种特殊角色叫 **行内块元素 (inline-block)**（如 `<img>`、`<input>`），它们既能在一行排列，又可以设置宽高。

---

## 3. 标准流的三个重要特性

### ① 从上到下，从左到右

这是文档流的基本方向。在中文/英文环境下，它是从左上角开始的。而在一些从右向左阅读的语言（如阿拉伯语）中，标准流的方向会有所调整。

### ② 空间占据 (Space Occupying)

在标准流中的每个元素都会在页面上占据一个确定的空间。这意味着：

- 后面的元素会自动避开前面的元素。
    
- 如果你不通过 CSS 移动它，它会老老实实地待在 HTML 源码所在的位置。
    

### ③ 边距重叠 (Margin Collapse)

这是标准流中一个比较奇特的行为：当两个垂直堆叠的**块级元素**相遇时，它们的上下外边距（margin）会发生重叠，最终间距取两者中的较大值，而不是相加。

---

## 4. 为什么要“脱离”标准流？

标准流虽然稳定，但灵活性不足。如果我们想实现以下效果，就必须让元素**脱离标准流**：

- 让两个 `div` 并排显示（过去用 `float`）。
    
- 让一个按钮始终固定在网页右下角（使用 `position: fixed`）。
    
- 让一个图片浮动在文字中间（使用 `float`）。
    
- 重叠两个元素（使用 `position: absolute`）。
    

### 脱离标准流的手段：

1. **浮动 (`float`)**：元素浮起来，不再占据标准流的空间，但文字会环绕它。
    
2. **绝对定位 (`position: absolute/fixed`)**：元素彻底“飞”出了文档层，不再对标准流中的其他元素产生任何挤压影响。
    

---

## 5. 总结对比表

|**特性**|**块级元素 (Block)**|**行内元素 (Inline)**|**脱离流元素 (Float/Absolute)**|
|---|---|---|---|
|**换行**|自动换行，独占一行|不换行，并排排列|改变默认排列方式|
|**宽高**|可设置|由内容决定，不可设置|可设置|
|**默认宽度**|容器的 100%|内容宽度|内容宽度（包裹性）|
|**空间占据**|占据标准流空间|占据标准流空间|**不占据**标准流空间|

---

### 下一步建议

理解标准流是学习 CSS 的第一步。为了更深入理解，**您是否想让我为您演示一下当一个元素“脱离”标准流后，周围的元素是如何重新排列的？**