###   开启命名空间

在模块对象内设置`namespaced: true`

```js
const moduleShopCar = {
    namespaced: true, //开启了命名空间
    // ->mutations/actions/getters的使用就有了区别
    state () {},
    mutations: {},
    actions: {},
    getters: {},
    modules: {}
}
```