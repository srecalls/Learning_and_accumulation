## 变量
1. 声明多个变量
```ts
let age = 18, uname = 'pink'
```

## 对象
1. 遍历对象
```js
for (let k in obj) {
	console.log(k)
	console.log(obj[k])
	// 不用console.log(obj.k)的原因是因为'k'是字符串
}
```

2. for……in和for……of的区别
for……in 里 item 是 键 key
for……of 里 item  是 值 value
```js
var numbers = ['一','二','三']

for(let num of numbers){
	console.log(num); // '一','二','三'
}

for(let num in numbers){
	console.log(num); // 0, 1, 2
}
```

## 函数
1. 匿名函数


## 类型转换
1. 显式Boolean对象转换
```js
记忆:
"", 0, undefined, null, false, NaN转换为布尔值后都为false，其余则为true
console.log(Boolean("")) // false
console.log(Boolean(0)) // false
console.log(Boolean(undefined)) //false
console.log(Boolean(null)) // false
console.log(Boolean(false)) // false
console.log(Boolean(NaN)) // false
```

2. 隐式转换
```js
记忆：
1. 有字符串的加法+1，结果是""
2. 减法-(像大多数数学运算一样)只能用于数字，它会使空字符串""转换为0
3. null 经过数字转换之后会变为 0
4. undefined 经过数字转换之后会变为 NaN
console.log(Number(null)) // 0
console.log(Number(undefined)) // NaN
```

## 数据类型
1. null 类型开发场景
```js
如果一个变量里确认存放的是"对象"，如果还没准备好对象，可以放个null
```


2. 检测数据类型
```js
typeof a可以
typeof(a)也可以
```
