**<center>网络工程1班
小组成员：何子亨、黄润、黎炜堂</center>**


#### 一、项目背景
在电影和电视剧等影视作品发布后，豆瓣上的用户往往会对其进行评论，这些评论包含了很多有价值的信息，例如观众对影视作品的评价、影视作品的类型等。因此，我们希望通过爬取豆瓣影视作品的评论，并使用开源分析框架对其进行分类和分析，以便更好地了解用户对影视作品的评价和市场需求。

#### 二、实现方法
本项目使用Python语言编写爬虫程序，通过豆瓣获取影视作品的评论，并将其存储在非关系数据库中。我们选择使用MongoDB作为数据库，因为它具有高可扩展性和灵活性。同时，我们还使用了开源分析框架Flink、Mahout和Spark，对评论进行分类和分析。

#### 三、实现步骤

##### 1.  爬取豆瓣影视作品的评论

爬虫程序使用Python编写，通过豆瓣获取影视作品的评论。具体来说，我们使用requests库发送HTTP请求，并解析返回的html数据。在请求时，我们需要注意一些限制，例如每个IP地址每分钟只能发送150个请求。因此，我们设置了一些延迟和随机时间，以避免被豆瓣屏蔽。并选择抓取评论的数量，保证数据量达到要求

在解析html数据时，我们提取了评论的一些关键信息，例如评论的ID、用户ID、评分、评论内容等。为了保证数据的质量和完整性，我们还对评论内容进行了一些处理，例如去除HTML标签和特殊字符、分词等。

主要代码
```python
def save_movie(subject_id, movie_json):
    subject_url = "https://movie.douban.com/subject/" + subject_id
    subject_res = requests.get(url=subject_url, headers=head)
    subject_soup = BeautifulSoup(subject_res.content, "html.parser")

    info_soup = subject_soup.find(id="info")
    info_json = {}

    info_arr = re.findall(r'>(.*?)<', str(info_soup))
    info_arr = [x for x in info_arr if x and x != " / " and x != ": " and x != ", " and x != " "]

    i = [0]
    helper(info_json, info_arr, i, "导演", lambda x: x != "编剧")
    helper(info_json, info_arr, i, "编剧", lambda x: x != "主演")
    helper(info_json, info_arr, i, "主演", lambda x: x != "类型:")
    helper(info_json, info_arr, i, "类型:", lambda x: len(x) < 5)

    while i[0] + 1 < len(info_arr):
        if info_arr[i[0]] == "季数:":
            helper(info_json, info_arr, i, "季数:", lambda x: x.isdigit())
        else:
            info_json[info_arr[i[0]][0:-1]] = info_arr[i[0] + 1]
            i[0] = i[0] + 2

    movie_json["id"].append(subject_id)
    if subject_soup.find(class_="episode_list"):
        movie_json["类别"].append("电视剧")
    else:
        movie_json["类别"].append("电影")
    movie_json["名称"].append(subject_soup.find(property="v:itemreviewed").text)
    movie_json["年份"].append(subject_soup.find(class_="year").text[1:-1])
    movie_json["海报链接"].append(subject_soup.find(class_="subject clearfix").find("img").attrs["src"])
    movie_json["简介"].append(subject_soup.find(property="v:summary").text)
    helperpop(info_json, movie_json, "导演")
    helperpop(info_json, movie_json, "编剧")
    helperpop(info_json, movie_json, "主演")
    helperpop(info_json, movie_json, "类型")
    movie_json["更多"].append(str(info_json))
```

```python
def save_comment(subject_id, comment_json):
    comment_url = "https://movie.douban.com/subject/" + subject_id + "/comments?limit=600&status=P&sort=new_score"
    comment_res = requests.get(url=comment_url, headers=head)
    comment_soup = BeautifulSoup(comment_res.content, "html.parser")

    comments = comment_soup.findAll(class_="comment")

    for comment in comments:
        comment_json["id"].append(comment_index[0])
        comment_json["movie_id"].append(subject_id)
        comment_json["votes"].append(int(comment.find(class_="votes vote-count").text))
        comment_json["usernames"].append(comment.findAll("a")[1].text)
        comment_json["times"].append(comment.find(class_="comment-time").text.strip())
        comment_json["locations"].append(comment.find(class_="comment-location").text)
        star = comment.select("[class*=allstar]")
        if star:
            comment_json["rates"].append(int(star[0].attrs["class"][0][7:9]))
        else:
            comment_json["rates"].append(int(30))
        comment_json["contexts"].append(comment.find(class_="short").text)
	    comment_index[0] = comment_index[0] + 1
```
##### 2.  将评论存储到MongoDB中
我们选择了MongoDB作为数据库，因为它能够存储非结构化数据，并且具有高可扩展性。在Python中，我们使用PyMongo库连接MongoDB，并将评论存储到其中。具体来说，我们创建了一个名为"comments"的集合，每个评论都作为一个文档存储在其中。每个文档包含了评论的一些关键信息，例如评论的ID、用户ID、评分、评论内容等。同时，我们还使用了MongoDB的索引功能，在集合中创建了一些索引，以提高查询效率。

方便查询评论的对应的影视作品，还创建了一个名为“movie”的集合，保存了影视作品的信息

![[Pasted image 20230430165443.png]]
![[Pasted image 20230430165510.png]]


#### 四、初步结果

在实现中期目标的过程中，我们已经成功爬取了豆瓣影视作品的评论，并将其存储在MongoDB中。

下一步，我们将继续完善项目，包括优化爬虫程序。

#### 五、后续目标
##### 1. 数据清洗和特征提取
对评论数据进行清洗和特征提取，以便将其转换为可供机器学习算法使用的格式。使用Spark的DataFrame API进行数据清洗和特征提取，例如tokenizer和StopWordsRemover对评论进行分词和去除停用词，并使用HashingTF或CountVectorizer将评论转换为特征向量。

##### 2. 模型训练和评估
选择一个分类器模型，并使用Spark的MLlib API进行模型训练和评估。你可以选择逻辑回归、朴素贝叶斯、支持向量机等模型，使用CrossValidator进行模型选择和调优

##### 3.可视化输出  
最后，使用Spark的DataFrame API和第三方可视化库（例如Bokeh或Matplotlib）生成可视化HTML输出，以展示分类结果。使用HTML和CSS来自定义输出的样式和布局