对于标签多加class，会对浏览器的速度产生一些影响，但通常不会造成明显的性能问题。

在处理DOM时，浏览器需要解析HTML代码并构建DOM树，然后将CSS样式应用于每个元素。当元素具有多个类时，浏览器需要处理这些类并将相应的样式应用于元素。这个过程需要一些时间，但在大多数情况下，处理标签的类不会对页面的性能产生显著影响。

然而，如果一个页面有大量的元素，并且每个元素都有多个类，那么这可能会增加页面的加载时间和渲染时间，从而导致用户体验下降。因此，在编写HTML和CSS代码时，建议尽可能避免过度使用类，只使用必要的类来实现所需的样式和功能。