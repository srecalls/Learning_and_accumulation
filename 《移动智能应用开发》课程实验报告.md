班级: 网络工程一班    
姓名：黄润    
学号：20202132038    
设计时间: 2023-06-10

## 一、项目的名称

To-Do List 待办事项

## 二、项目的主题及设计目的

该项目的定位是简单、易用、免费的To-Do List应用

应用程序的主要目的是帮助人们有效地管理他们的时间和任务

- 行业市场分析：备忘录已成为一款非常流行的应用，它可以帮助人们有效地管理时间和任务。在智能手机用户中，备忘录应用的需求越来越高。据预测，全球移动备忘录类应用市场的复合年增长率将达到 19.3％，市场规模将从 2018 年的 28.7 亿美元增长到 2023 年的 63.5 亿美元。
- 竞争对手或同类产品分析： 目前市场上已有许多 To-Do List 应用，包括 Todoist、TickTick、Wunderlist、Microsoft To-Do 等。但是，这些应用通常需要高昂的订阅费用才能解锁高级功能。此外，它们的界面可能过于复杂或不符合用户需求。
- 自身条件分析： Android Studio 提供了一个全面的开发平台，可用于创建 To-Do List 应用。 Kotlin 作为一种高级编程语言，具有易读、易维护和易扩展的特性。此外，Google Play 商店提供了良好的分发平台，可将应用程序推广给全球用户。

## 三、软件简介

- **软件基本结构：**

一共有4个活动及页面，分别是以下内容

**主页面**：待办事项的列表展示，登录按钮、添加事项按钮，各事项的计时以及删除

**添加事项页面**：事项类型、名称、时间等属性修改添加

**登录页面**：输入账号密码登录，以及注册按钮

**注册页面**：输入账号密码注册

- **基本内容：**

实现用户登陆注册、以及事项添加、删除和计时功能

- **运行环境：**

Android SDK 33

## 四、负责部分

主要负责部分：登录界面与注册界面

#### 1.登录界面功能代码

```kotlin
package com.example.myapplication2

import android.content.Intent
import android.content.res.Configuration
import androidx.appcompat.app.AppCompatActivity
import android.os.Bundle
import android.text.Editable
import android.text.TextWatcher
import android.widget.Button
import android.widget.EditText
import android.widget.Toast
import java.util.*

// 定义一个数据类 State，用于存储某个输入框的状态值
data class State(var value: Int)

// 主活动类 MainActivity3
class MainActivity3 : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_main3)

        // 获取用户名输入框、密码输入框和登录按钮的实例
        val editText_username = findViewById<EditText>(R.id.username)
        val editText_password = findViewById<EditText>(R.id.password)
        val button_login = findViewById<Button>(R.id.login)

        // 定义用户名输入框、密码输入框和登录按钮的状态值
        var usernameState = State(1)
        var passwordState = State(1)
        var loginState = State(-2)

        // 监听用户名输入框和密码输入框的文本变化，根据输入的内容更新相应的状态值
        checkText(editText_username, usernameState, ::usernameValid, button_login, loginState)
        checkText(editText_password, passwordState, ::passwordValid, button_login, loginState)

        // 监听登录按钮的点击事件，如果用户名和密码都正确，则登录成功，否则登录失败
        button_login.setOnClickListener {
            val username = editText_username.text.toString()
            val password = editText_password.text.toString()
            if (username == "123456" && password == "123456") {
                Toast.makeText(this, "登陆成功", Toast.LENGTH_LONG)
                finish()
            } else {
                Toast.makeText(this, "登陆失败", Toast.LENGTH_LONG)
            }
        }

        // 获取注册按钮的实例，并监听其点击事件，跳转到注册页面
        val button_register = findViewById<Button>(R.id.register)
        button_register.setOnClickListener{
            val intent = Intent(this, MainActivity4::class.java)
            startActivity(intent)
        }

        // 获取语言切换按钮的实例，并监听其点击事件，切换应用程序的语言环境
        val button_language = findViewById<Button>(R.id.button_language)
        button_language.setOnClickListener {
            // 根据当前语言环境切换到另一种语言环境
            var locale: Locale = if (button_language.text != "ENG") { Locale("zh") }
            else { Locale("en") }
            Locale.setDefault(locale)
            val config = Configuration()
            config.setLocale(locale)
            resources.updateConfiguration(config, resources.displayMetrics)
            val intent = packageManager.getLaunchIntentForPackage(packageName)
            intent?.addFlags(Intent.FLAG_ACTIVITY_CLEAR_TOP)
            startActivity(intent)
        }
    }

    // 判断用户名是否合法的函数，如果合法返回 1，否则返回 -1
    private fun usernameValid(username : String) : Int {
        if (username.length !in 5..15) return -1
        return 1
    }

    // 判断密码是否合法的函数，如果合法返回 1，否则返回 -1
    private fun passwordValid(password : String) : Int {
        if (password.length !in 5..15) return -1
        return 1
    }

    // 监听输入框的文本变化，并根据输入的内容更新相应的状态值
    private fun checkText(editText: EditText, state : State, onChangeFun : (String)->Int, button: Button, loginState: State) {
        editText.addTextChangedListener(object : TextWatcher {
            override fun beforeTextChanged(s: CharSequence?, start: Int, count: Int, after: Int) {
            }

            override fun onTextChanged(s: CharSequence?, start: Int, before: Int, count: Int) {
                if (onChangeFun(s.toString()) == 1 && state.value == 1) {
                    loginState.value += 1
                    state.value = 0
                }
                if (loginState.value >= 0) button.isEnabled = true
            }

            override fun afterTextChanged(s: Editable?) {
            }
        })
    }
}
```

##### 功能描述：
1. `data class State(var value: Int)`：定义了一个数据类 State，用于存储输入框的状态值。State 类包含一个 Int 类型的 value 属性，表示输入框的状态值。

2. `override fun onCreate(savedInstanceState: Bundle?)`：创建了一个名为 onCreate 的函数，用于在 Activity 创建时初始化界面和事件监听器。该函数接收一个 Bundle 类型的 savedInstanceState 参数，表示恢复之前保存的状态。

3. `setContentView(R.layout.activity_main3)`：设置界面布局为 activity_main3.xml。

4. `val editText_username = findViewById<EditText>(R.id.username)`：获取用户名输入框实例。findViewById 函数根据 ID 查找对应的 View，返回一个泛型类型为 EditText 的实例。

5. `val editText_password = findViewById<EditText>(R.id.password)`：获取密码输入框实例。findViewById 函数根据 ID 查找对应的 View，返回一个泛型类型为 EditText 的实例。

6. `val button_login = findViewById<Button>(R.id.login)`：获取登录按钮实例。findViewById 函数根据 ID 查找对应的 View，返回一个泛型类型为 Button 的实例。

7. `var usernameState = State(1)`、`var passwordState = State(1)`、`var loginState = State(-2)`：定义了三个 State 类型的变量，用于存储用户名输入框、密码输入框和登录按钮的状态值。初始状态值为 1 或 -2。

8. `checkText(editText_username, usernameState, ::usernameValid, button_login, loginState)`、`checkText(editText_password, passwordState, ::passwordValid, button_login, loginState)`：调用 checkText 函数，监听用户名输入框和密码输入框的文本变化，并根据输入的内容更新相应的状态值。

9. `button_login.setOnClickListener`：监听登录按钮的点击事件。如果用户名和密码正确则登录成功，否则登录失败。根据不同的情况弹出相应的 Toast。

10. `val button_register = findViewById<Button>(R.id.register)`：获取注册按钮实例。findViewById 函数根据 ID 查找对应的 View，返回一个泛型类型为 Button 的实例。

11. `button_register.setOnClickListener`：监听注册按钮的点击事件。点击后跳转到注册页面。

12. `val button_language = findViewById<Button>(R.id.button_language)`：获取语言切换按钮实例。findViewById 函数根据 ID 查找对应的 View，返回一个泛型类型为 Button 的实例。

13. `button_language.setOnClickListener`：监听语言切换按钮的点击事件。点击后根据当前语言环境切换到另一种语言环境，然后重启应用程序。

整个功能的具体过程是：

用户在用户名输入框和密码输入框中输入内容时，监听其文本变化，根据输入内容更新相应的状态值。如果输入内容符合要求，则将相应的状态值从 1 改为 0，当用户名输入框和密码输入框的状态值都为 0 时，将登录按钮状态设为可用。

用户点击登录按钮时，判断用户名和密码是否正确。如果正确则登录成功，否则登录失败。用户点击注册按钮时，跳转到注册页面。

5. 用户点击语言切换按钮时，根据当前语言环境切换到另一种语言环境，然后重启应用程序。


#### 2. 注册页面功能代码
```kotlin
package com.example.myapplication2

import androidx.appcompat.app.AppCompatActivity
import android.os.Bundle
import android.widget.Button

class MainActivity4 : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_main4)

        // 获取注册按钮实例
        val button_registered = findViewById<Button>(R.id.register)
        // 监听注册按钮的点击事件，点击后销毁当前 Activity，返回上一个 Activity
        button_registered.setOnClickListener  {
            finish();
        }
    }
}
```


##### 功能描述：
具体功能包括：
设置界面布局为 activity_main4.xml，获取注册按钮实例，监听注册按钮的点击事件，点击后销毁当前 Activity，返回上一个 Activity。
    

整个功能的具体过程是：
用户进入注册界面后，可以点击返回上一个界面， 用户点击返回按钮时，销毁当前 Activity，返回上一个 Activity。

### 关键技术
1. XML 布局文件：在代码中通过 setContentView 函数设置不同的布局文件，来显示不同的用户界面。
2. Activity：在代码中定义了多个 Activity，每个 Activity 都对应一个用户界面，包含了各种 UI 组件和事件处理逻辑。
3. Button：在代码中使用了多个 Button，通过设置 Button 的属性和事件监听器，来实现不同的功能。
4. TextWatcher：在代码中实现了多个 TextWatcher 接口的方法，来监听文本输入框中的文本变化，实现输入校验和状态更新等功能。
5. Intent：在代码中使用了 Intent，通过设置 Intent 的参数和标志，来实现不同 Activity 之间的跳转、数据传递和操作执行等功能。
6. Toast：在代码中使用了 Toast，通过设置 Toast 的文本、时长和位置等属性，来显示短暂的通知信息。
7. Configuration：在代码中使用了 Configuration，通过设置 Configuration 的属性，来实现应用程序的多语言和字体大小适配等功能。

## 五、目前存在的问题
MainActivity3 中的 checkText 函数实现比较繁琐，有更好的改进方案
    
MainActivity3 中的 checkText 函数实现中，当文本输入框中的文本变化时，每次都会创建一个新的 Runnable 对象，可能会导致内存泄漏和性能问题。
    
MainActivity3 中的 checkText 函数实现中，输入校验逻辑比较简单，会存在漏洞或者误判的情况。
    
MainActivity4 中的代码比较简单，仅仅实现了一个返回上一个 Activity 的功能，并没有完全实现注册功能只能提供返回

## 六、自我感想
这个项目让我深入了解了 Android 应用程序开发的基础知识，同时学会了如何使用 Android Studio 和 Kotlin 来开发应用程序。在实现主界面的过程中，我学会了如何使用布局文件、控件和适配器等组件，以及如何处理视图和数据的交互。我也掌握了如何使用点击事件监听器和意图等机制，来实现界面之间的转换和通信。

虽然在开发过程中遇到了一些问题，例如如何正确地布局控件、如何处理列表的数据绑定、如何处理用户输入等，但是通过查阅 Android 开发文档、搜索网络资源和向同学请教，我最终找到了解决问题的方法，并将其应用到了代码中。我也意识到了一些潜在的问题，例如输入验证、错误处理和安全性等问题，我将在后续的开发中逐步解决这些问题，以提高应用程序的稳定性和用户体验。

在这个项目中，我发现了自己的不足之处，例如对某些 Android 组件和机制的理解还不够深入，对某些设计模式和编程技巧的掌握还不够熟练。因此，我将继续努力学习和实践，不断提升自己的开发能力和水平。同时，我也认识到 Android 应用程序开发需要耐心和细心，需要不断学习和探索，需要不断改进和优化，这也是我未来在这方面需要持续关注和努力的方向。