![[如何判断一个对象为空.png]]

## 方法一 JSON.stringify()
```js
function isEmpty(obj) {
	return JSON.stringify(obj) === '{}'
}
```

## 方法二 Object.key()
```js
function isEmpty(obj) {
	return Object.keys(obj).length === 0
}
```

## 方法三 Object.getOwnPropertyNames()
```js
function isEmpty(obj) {
	return Object.getOwnPropertyNames(obj).length === 0
}
```

## 方法四 for (const key in obj) {}
```js
function isEmpty(obj) {
	for (const key in obj) {
		if (key) return false
	}
	return true
}
```

上面四个方法都会有一个问题
当我的对象里为这样时
```js
const key = Symbol('a')
const obj = {
	[key]: 1
}
```
以上函数全部返回true，但对象里不为空

因此建议使用
![[如何判断一个对象为空-1.png]]
去判断
