## 登录模块怎么做的
 1. 在登录页点击登录的时候，前端会带着用户名和密码去调用后端的登录接口

2. 后端收到请求，验证用户名和密码，若验证失败，返回错误信息，前端提示相应的错误信息。如果验证成功，就会给前端返回一个token

3. 前端拿到token，将token存储到vuex和localStorage中，并跳转页面，即登录成功

4. 前端每次跳转至需要具备登录状态页面时，都需要判断当前token是否存在，不存在就跳转到登录页，存在则正常跳转（通常封装在路由守卫中）

5. 最后，前端在向后端发送其他请求的时候，需要在请求头中带上token（项目中通常封装在请求拦截器中），后端判断请求头中有无token，有则验证该token，验证成功就正常返回数据，验证失败（如已过期）则返回相应错误码。前端拿到错误信息，清除token并回退至登录页

## 补充后续面试官继续追问的问题（对登录流程进一步的梳理和思考）

1. 这个token的值是我们在登录也就是login.vue这个页面从后端获得的。但是我们利用这个token值获取用户信息是在另一个页面（个人中心，我的）。也就是说我们在登录页面获取token，在“我的”页面通过token请求用户名放到对应位置

2. 在登录界面我们主要是通过localstorage来把token值存到本地。核心代码：

localStorage.setItem("token",res.data.data.token)
//表示把res.data.data.token这个value存储到本地的“token”这个key里面

3. 什么时候在localStorage中取token？

```js
created(){
    	//页面加载时就从本地通过localstorage获取存储的token值
        this.token =  localStorage.getItem('token')
    },
```
在created中，此时数据已经创建完毕，可以在localStorage中拿到持久化存储的token


要实现微信小程序中的扫码登录和手机验证码登录，你需要进行以下步骤：

1. 扫码登录：
   - 在微信开放平台注册开发者账号，并创建一个小程序应用。
   - 在小程序中添加扫码登录功能，通常使用微信开放平台提供的 `wx.login` 接口获取登录凭证 code。
   - 将获取的 code 发送到服务器端，在服务器端使用微信开放平台提供的接口验证 code 的有效性，并获取用户的唯一标识符（openid）等信息。
   - 根据需要，你可以将用户的唯一标识符与你的系统中的用户进行关联，实现登录功能。

2. 手机验证码登录：
   - 在你的后端服务器上设置短信服务，如阿里云短信服务或腾讯云短信服务等。
   - 在小程序中提供一个手机号输入框和发送验证码按钮。
   - 当用户输入手机号并点击发送验证码按钮时，小程序将手机号发送到服务器端。
   - 服务器端通过短信服务发送验证码到用户的手机号。
   - 用户在小程序中输入收到的验证码，并点击登录按钮。
   - 将手机号和验证码发送到服务器端，服务器端进行验证。
   - 如果验证码验证通过，服务器端生成用户凭证，返回给小程序并保存在本地，实现登录功能。

需要注意的是，这些步骤中涉及到的具体实现细节会依赖于你所使用的开发框架和后端技术。你需要根据自己的开发环境和需求，查阅相关文档和示例代码来完成具体实现。

此外，为了保障用户信息安全，建议在登录过程中使用安全策略，如加密传输、防止恶意攻击等。同时，在使用手机号登录时，你也需要遵守相关法律法规，合法合规地处理用户的个人信息。

web端? ?
小程序?微信扫码登录?
手机验证码登录??
uni-app 登录??
简历上的项目是什么样，当前和面试官谈到了哪个项目??
我这个项目有 xx 种登录方式: 第一种是xxx，第二种是xxxx....
手机号码登录:用户点击登录按钮，校验数据通过 (用户手机号，图片验证码，手机验证码)，进行登录请求 (进行一定的封装)，获取token之后做本地保存，获取用户数据进行保存，其他业务操作(关闭登录框)。跳转到某个登录之后的页面需要去进行路由守卫
微信扫码登录:根据微信扫码业务文档去做 (第三方登录，参考流程图去梳理
项目类型不一样，回答不一样 (前台项目，后台管理系统，权限管理)