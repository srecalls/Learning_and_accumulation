## 什么是XSS攻击

[[Web安全课程实验]]

### 概念1

#### XSS漏洞概念

```
	跨站脚本攻击（Cross Site Scripting），为了不和层叠样式表（Cascading Style Sheets，缩写：CSS）混淆，所以架构跨站脚本攻击缩写为XSS。XSS就是攻击者在web页面插入恶意的Script代码，当用户浏览该页面时，嵌入其中的js代码会被执行，从而达到恶意攻击的目的。某种意义上也是一种注入攻击，是指攻击者在页面中注入恶意的脚本代码，当受害者访问该页面时，恶意代码会在其浏览器上执行，需要强调的是，XSS不仅仅限于JavaScript，还包括flash等其它脚本语言。
```

#### XSS的类型

##### 1.反射型XSS

```
反射型XSS只是简单的把用户输入的数据“反射”给浏览器，也就是需要诱使用户“点击”一个恶意链接，才能攻击成功。反射型XSS也叫作“非持久性XSS”
```

##### 2.存储型XSS

```
存储型XSS会把用户输入的数据“存储”在服务器端。如果没有过滤或者过滤不严，那么这些恶意代码被服务器端接收并存储，用户访问该页面的时候触发代码执行。这种XSS具有很强的稳定性，当再次访问页面时会被执行。<持久化>.这种XSS比较危险，容易造成蠕虫，盗窃cookie等。
存储型XSS和反射型XSS、DOM型XSS相比，具有更高的隐蔽性，危害性也更大。它们之间最大的区别在于反射型XSS与DOM型XSS执行都必须依靠用户手动去触发，而存储型XSS却不需要。
```

##### 3.DOM型XSS

```
DOM的全称为Document Object Model，即文档对象模型，DOM通常用于代表在HTML、XHTML和XML中的对象。使用DOM可以允许程序和脚本动态低访问和更新文档的内容、结构和样式。
实际上，这种类型的XSS并非按照“数据是否保存在服务器端”来划分的，从效果上来说也是反射型XSS单独划分出来的，因为DOM Based XSS的形成原因比较特别。这是由于客户端脚本自身解析不正确导致的安全问题。
这种利用也需要受害者点击链接来触发，DOM型XSS是前端代码中存在了漏洞（DOM型的XSS是不需要与服务器交互的，它只发生在客户端处理数据阶段），而反射型是后端代码中存在了漏洞。
反射型和存储型xss是服务器端代码漏洞造成的，payload在响应页面中，在dom xss中，payload不在服务器发出的HTTP响应页面中，当客户端脚本运行时（渲染页面时），payload才会加载到脚本中执行。
```

### 概念2
#### （1）概念
XSS 攻击指的是跨站脚本攻击，是一种代码注入攻击。攻击者通过在网站注入恶意脚本，使之在用户的浏览器上运行，从而盗取用户的信息如cookie等。

XSS的本质是因为网站没有对恶意代码进行过滤，与正常的代码混合在一起了，浏览器没有办法分辨哪些脚本是可信的，从而导致了恶意代码的执行。

攻击者可以通过这种攻击方式可以进行以下操作:
- 获取页面的数据，如DOM、cookie、localStorage;
- DOS攻击，发送合理请求，占用服务器资源，从而使用户无法访问服务器;。
- 破坏页面结构;
- 流量劫持(将链接指向某网站);

#### （2）攻击类型
XSS可以分为存储型、反射型和DOM型:
- 存储型指的是恶意脚本会存储在目标服务器上，当浏览器请求数据时，脚本从服务器传回并执行.
- 反射型指的是攻击者诱导用户访问一个带有恶意代码的URL后，服务器端接收数据后处理，然后把带有恶意代码的数据发送到浏览器端，浏览器端解析这段带有XSS代码的数据后当做脚本执行，最终完成XSS攻击。
- DOM型指的通过修改页面的 DOM节点形成的 XSS.


##### （1）存储型XSS的攻击步骤:
- 1.攻击者将恶意代码提交到目标网站的数据库中。
- 2.用户打开目标网站时，网站服务端将恶意代码从数据库取出，拼接在HTML 中返回给浏览器。
- 3.用户浏览器接收到响应后解析执行，混在其中的恶意代码也被执行。
- 4.恶意代码窃取用户数据并发送到攻击者的网站，或者冒充用户的行为，调用目标网站接口执行攻击者指定的操作。

这种攻击常见于带有用户保存数据的网站功能，如论坛发帖、商品评论、用户私信等。

##### （2）反射型XSS的攻击步骤:
- 1.   攻击者构造出特殊的URL，其中包含恶意代码。
- 2.   用户打开带有恶意代码的URL时，网站服务端将恶意代码从URL中取出，拼接在HTML中返回给浏览器。
- 3．用户浏览器接收到响应后解析执行，混在其中的恶意代码也被执行。
- 4.   恶意代码窃取用户数据并发送到攻击者的网站，或者冒充用户的行为，调用目标网站接口执行攻击者指定的操作。


反射型XSS跟存储型XSS的区别是:存储型XSS的恶意代码存在数据库里，反射型XSS的恶意代码存在URL里。

反射型XSS漏洞常见于通过URL传递参数的功能，如网站搜索、跳转等。由于需要用户主动打开恶意的URL才能生效，攻击者往往会结合多种手段诱导用户点击。


##### （3） DOM型XSS的攻击步骤:
- 1．攻击者构造出特殊的URL，其中包含恶意代码。
- 2．用户打开带有恶意代码的URL。
- 3.   用户浏览器接收到响应后解析执行，前端JavaScript取出 URL 中的恶意代码并执行。
- 4．恶意代码窃取用户数据并发送到攻击者的网站，或者冒充用户的行为，调用目标网站接口执行攻击者指定的操作。

DOM型XSS 跟前两种XSS的区别: DOM型XSS攻击中，取出和执行恶意代码由浏览器端完成，属于前端JavaScript自身的安全漏洞，而其他两种XSS都属于服务端的安全漏洞。


### XSS攻击触发条件
了解XSS的触发条件就先得从HTML（超文本标记语言）开始

我们浏览的网页全部都是基于超文本标记语言创建的，而XSS的原理也就是往HTML中注入脚本，HTML指定了脚本标记\<script>\</script>

在没有过滤字符的情况下，只需要保持完整无错的脚本标记即可触发XSS

#### XSS触发方式（1）

假如我们在某个资料表单提交内容,表单提交内容就是某个标记属性所赋的值，我们可以构造如下值来闭和标记来构造完整无错的脚本标记
```js
><script>alert('XSS');</script><
```

接触过javascript的同志应该知道，javascript有一个URL伪协议，可以使用“javascript：”这种协议说明符加上任意的javascript代码，当浏览器装载这样的URL时，便会执行其中的代码.于是我们就得出了一个经典的XSS示例：
```js
<img src="javascript:alert('XSS');">
```

#### XSS触发方式（2）

事件也能帮助我们触发XSS
例如当img标记内含有一个onerror()事件而正好图片没有正常输出便会触发这个事件，而事件中可以加入任意的脚本代码，其中的代码也会执行.
```js
<img src="" onerror=alert('XSS')>
```

# DVWA
## XSS(DOM)
### 级别：Low
#### 1.查看源码
![[Pasted image 20230619131253.png]]
DOM xss low 服务器端没有任何php代码，执行命令的只有客户端的js代码。

#### 2.点击English
![[Pasted image 20230619131416.png]]
我们发现url地址栏上出现了English

#### 3.尝试注入js代码
![[Pasted image 20230619131452.png]]
![[Pasted image 20230619131616.png]]


```js
<script>alert('xss')</script>
```

### 级别：Medium
#### 1.查看源码
![[Pasted image 20230619145124.png]]
发现对script进行了过滤
```php
<?php  
  
// Is there any input?  
if ( array_key_exists( "default", $_GET ) && !is_null ($_GET[ 'default' ]) ) {    $default = $_GET['default'];        # Do not allow script tags    if (stripos ($default, "<script") !== false) {        header ("location: ?default=English");  
        exit;  
    }  
}  
  
?>
```
这段代码是一个PHP脚本，用于检查HTTP GET请求中是否有名为"default"的参数，如果存在并且不为null，则将其存储在变量$default中。

接下来，代码使用了一个条件语句来检查$default中是否包含"<script"字符串，如果包含，则认为是恶意输入，可能会导致跨站脚本攻击（XSS攻击），因此代码会将浏览器重定向到一个默认页面（这里是"?default=English"）并退出脚本。

这段代码的目的是防止恶意用户通过输入含有恶意脚本的参数来攻击网站。它使用了一个简单的方法来检查输入中是否包含恶意脚本，并将用户重定向到一个安全页面。但是它并没有对其他类型的攻击进行保护，比如SQL注入等，因此，这段代码可能需要结合其他安全措施来保护Web应用程序。


#### 2.检查前端html语句
![[Pasted image 20230619145538.png]]

value的值是从这个当中选出来的，那么我们可以通过构造闭合来执行我们的语句。  
把English替换成 
```js
></option></select><img src=1 onerror=alert(/xss/)>
```

```js
</option></select><img src=1 onerror=alert(/xss/)>
```



这段语句是一个恶意的XSS攻击代码，它的目的是在浏览器中执行一个JavaScript脚本，弹出一个警告框，通常是用来窃取用户的敏感信息或者进行其他不良行为。

这个语句中包含了两个HTML标签：`<select>`和`<img>`。但是，这些标签并没有正确地闭合，而是通过输入`></option></select><img src=1 onerror=alert(/xss/)>`来构造了一个恶意的HTML代码片段。

在执行过程中，浏览器会尝试将这个HTML代码片段解析成一个DOM树，但是**由于标签没有正确闭合，浏览器会尝试自动纠正这个错误，将其视为一个整体标签，从而执行其中的JavaScript代码**。具体来说，浏览器会将这个HTML代码片段解析成以下HTML代码：

```html
<select>
  <option>
  </option>
</select>
<img src="1" onerror="alert(/xss/)">
```



虽然这段代码不是一个有效的HTML标记，但是浏览器会将其作为一个整体标记来执行，从而触发其中的JavaScript代码。这种技术被称为“标签欺骗”，它是一种常见的XSS攻击技术。


事件也能帮助我们触发XSS
例如当img标记内含有一个onerror()事件而正好图片没有正常输出便会触发这个事件，而事件中可以加入任意的脚本代码，其中的代码也会执行.
`<img src="" onerror=alert('XSS')>`

那么这个语句是怎么闭合的呢？  
学过html语言的我们都知道都是**双标签**，也就是说会识别两个标签之间的语句，那么我们就人为把它闭合起来，在执行过程中，**一旦存在一对标签**，那么就会执行这里面的语句，无法构成成**对标签**的不会执行里面的语句，而是单标签，也就是说他要把执行的语句涵盖在里面
![[Pasted image 20230619145800.png]]



### 级别：High
#### 1.查看源码
![[Pasted image 20230619151542.png]]

查看源码发现设置了白名单，只有白名单中的内容才会被执行。

```php
<?php  
  
// Is there any input?  
if ( array_key_exists( "default", $_GET ) && !is_null ($_GET[ 'default' ]) ) {    # White list the allowable languages    switch ($_GET['default']) {  
        case "French":  
        case "English":  
        case "German":  
        case "Spanish":            # ok            break;  
        default:            header ("location: ?default=English");  
            exit;  
    }  
}  
  
?>
```


这段代码是一个PHP脚本，用于检查HTTP GET请求中是否有名为"default"的参数，如果存在并且不为null，则进行一系列的检查。

首先，代码使用array_key_exists函数来检查$_GET数组中是否存在"default"键，如果存在，则执行后面的代码块。

在代码块中，使用switch语句来检查"default"参数的值是否在预定义的语言列表中，包括法语、英语、德语和西班牙语。如果参数的值在白名单中，则代码不会执行任何操作，如果参数的值不在白名单中，则代码会将浏览器重定向到一个默认页面（这里是"?default=English"）并退出脚本。

这段代码的目的是防止恶意用户通过输入不受支持的语言参数来攻击网站。它使用了一个白名单机制来检查输入的有效性，并将用户重定向到一个安全页面，以避免不必要的安全风险。但是，这段代码可能需要结合其他安全措施来保护Web应用程序，比如对输入进行更加严格的过滤和验证。


但这个闭合和之前的可不太一样。 我们在english后面加了注释符，很多人问了注释符不就代表后面的语句不会执行了吗，那闭合又有什么用呢。  
**哎，这就是DOM与其他的最大的区别了因为DOM是不会和后端的服务期进行交互的，它只是利用DOM解析树，而DOM解析树依旧会解析注释符后面的语句，只要解析了就可以成功执行。**  
在这里要注意的是，输入语句后，很多同学会疑惑为什么没反应，别慌，你没有错，这个时候你只需要刷新一下就好了。（但如果刷新不行，可能真的是你的语句有问题）

![[Pasted image 20230619151903.png]]

```js
English#></option></select><img src=1 onerror=alert(/xss/)>
```

这个输入可以被执行是因为在代码中使用了白名单机制，只允许预定义的语言，在这个输入中，`English#`是一个合法的白名单语言，所以不会被拒绝。而`></option></select><img src=1 onerror=alert(/xss/)>"`则是一个恶意的HTML代码片段，包含了一个未闭合的select标签和一个img标签，img标签中的`onerror`属性被设置为一个JavaScript代码，当图像加载失败时，这个JavaScript代码就会被执行。这种技术被称为"标签欺骗"，它是一种常见的XSS攻击技术。

在执行过程中，浏览器会尝试将这个HTML代码片段解析成一个DOM树，由于标签没有正确闭合，浏览器会尝试自动纠正这个错误，将其视为一个整体标签，从而执行其中的JavaScript代码。因此，当这个输入被提交到服务器时，服务器会将其视为一个合法的输入，并将其存储在变量中，当这个变量被用于后续的处理时，其中的JavaScript代码就会被执行，从而导致XSS攻击。


## XSS(Reflected)
### 级别：Low
#### 1.随便输入一下，查看页面显示内容
![[Pasted image 20230619153652.png]]

#### 2.查看源码，发现没有任何限制
![[Pasted image 20230619153744.png]]

```php
<?php  
  
header ("X-XSS-Protection: 0");  
  
// Is there any input?  
if( array_key_exists( "name", $_GET ) && $_GET[ 'name' ] != NULL ) {    // Feedback for end user    echo '<pre>Hello ' . $_GET[ 'name' ] . '</pre>';  
}  
  
?>
```

这段代码是一个简单的PHP脚本，它用于检查HTTP GET请求中是否有名为"name"的参数，如果存在并且不为null，则输出一个简单的欢迎消息。

在代码中，首先设置了一个HTTP响应头X-XSS-Protection: 0，这个响应头告诉浏览器不要尝试检测和阻止XSS攻击，这通常是为了避免一些过于严格的防御措施影响Web应用程序的正常运行。

接下来，代码使用array_key_exists函数来检查$_GET数组中是否存在"name"键，如果存在，则执行后面的代码块。

在代码块中，使用echo语句输出一个欢迎消息，包含用户输入的名称。由于代码没有对输入进行任何过滤或者验证，因此存在被恶意用户利用输入进行XSS攻击的风险。

总的来说，这段代码非常简单，只是一个用于输出欢迎消息的脚本。但是，它没有对输入进行任何处理，存在安全风险，因此在实际应用中，需要对输入进行更加严格的过滤和验证，以避免被攻击。同时，使用X-XSS-Protection响应头来保护Web应用程序免受XSS攻击也是一个不错的选择。

#### 3.在输入框中输入payload
```js
<script>alert('xss')</script>
```
![[Pasted image 20230619153937.png]]

#### 4.输入如下payload，拿到cookie
```js
<script>alert(document.cookie)</script>
```

![[Pasted image 20230619154003.png]]

### 级别：Medium
#### 1.查看源码
![[Pasted image 20230619154246.png]]

```php
<?php  
  
header ("X-XSS-Protection: 0");  
  
// Is there any input?  
if( array_key_exists( "name", $_GET ) && $_GET[ 'name' ] != NULL ) {    // Get input    $name = str_replace( '<script>', '', $_GET[ 'name' ] );    // Feedback for end user    echo "<pre>Hello {$name}</pre>";  
}

?>
```

这段代码是一个简单的PHP脚本，它用于检查HTTP GET请求中是否有名为"name"的参数，如果存在并且不为null，则输出一个简单的欢迎消息。

在代码中，首先设置了一个HTTP响应头X-XSS-Protection: 0，这个响应头告诉浏览器不要尝试检测和阻止XSS攻击，这通常是为了避免一些过于严格的防御措施影响Web应用程序的正常运行。

接下来，代码使用array_key_exists函数来检查$_GET数组中是否存在"name"键，如果存在，则执行后面的代码块。

在代码块中，首先使用str_replace函数来过滤用户输入中的"\<script>"字符串，将其替换为空字符串。这个处理方式虽然可以防止一些简单的XSS攻击，但是并不是非常安全，因为恶意用户可以使用其他的攻击向量来绕过这种过滤。

然后，代码使用echo语句输出一个欢迎消息，包含用户输入的名称。由于代码对输入进行了一定的过滤，因此可以防止一些简单的XSS攻击，但是仍然存在被攻击的风险。

总的来说，这段代码可以防止一些简单的XSS攻击，但是并不是非常安全，因此在实际应用中，需要对输入进行更加严格的过滤和验证，以避免被攻击。同时，使用X-XSS-Protection响应头来保护Web应用程序免受XSS攻击也是一个不错的选择。

由于str_replace函数把`<scrip>`标签替换为空，所以我们想办法绕过

#### 2.构造payload

Payload1:
```js
<scr<script>ipt>alert(1)</script>
```  
Payload2:
```js
<SCript>alert(1)</script>
```

以上两钟方法均可绕过

![[Pasted image 20230619154559.png]]

![[Pasted image 20230619154604.png]]

### 级别：High
#### 1.查看源码
![[Pasted image 20230619154744.png]]
```php
<?php  
  
header ("X-XSS-Protection: 0");  
  
// Is there any input?  
if( array_key_exists( "name", $_GET ) && $_GET[ 'name' ] != NULL ) {    // Get input    $name = preg_replace( '/<(.*)s(.*)c(.*)r(.*)i(.*)p(.*)t/i', '', $_GET[ 'name' ] );    // Feedback for end user    echo "<pre>Hello {$name}</pre>";  
}  
  
?>
```

已经对`<script>`标签进行了严格的过滤

这段代码是一个简单的PHP脚本，它用于检查HTTP GET请求中是否有名为"name"的参数，如果存在并且不为null，则输出一个简单的欢迎消息。

在代码中，首先设置了一个HTTP响应头X-XSS-Protection: 0，这个响应头告诉浏览器不要尝试检测和阻止XSS攻击，这通常是为了避免一些过于严格的防御措施影响Web应用程序的正常运行。

接下来，代码使用array_key_exists函数来检查$_GET数组中是否存在"name"键，如果存在，则执行后面的代码块。

在代码块中，首先使用preg_replace函数来过滤用户输入中的所有带有"script"字符的标签，将其替换为空字符串。这个处理方式可以防止大部分的XSS攻击，因为恶意用户往往会使用\<script>标签来注入JavaScript代码，从而实现攻击。

然后，代码使用echo语句输出一个欢迎消息，包含用户输入的名称。由于代码对输入进行了一定的过滤，因此可以防止大部分的XSS攻击。

总的来说，这段代码可以防止大部分的XSS攻击，但是并不能保证100%的安全，因此在实际应用中，需要对输入进行更加严格的过滤和验证，以避免被攻击。同时，使用X-XSS-Protection响应头来保护Web应用程序免受XSS攻击也是一个不错的选择。


#### 2.构造payload
Payload:
```js
<img src=1 onerror=alert(/xss/)>
```

![[Pasted image 20230619154959.png]]

通过上述执行我们会发现，每次注入payload是一次性的，关闭页面再次进入我们会发现不会再执行，这与后面的存储型xss最大的区别。

### XSS(Stored)
#### 法一
##### 1.写入payload，发现name栏有长度限制。
![[Pasted image 20230619155304.png]]

##### 2.打开开发者工具，对其进行修改
![[Pasted image 20230619155323.png]]
![[Pasted image 20230619155328.png]]

这个已经存储在数据库中了，每次刷新后都会弹出框，可重置数据库

#### 法二
也可以直接通过在message一栏输入
![[Pasted image 20230619155408.png]]
![[Pasted image 20230619155412.png]]


#### 法三
![[Pasted image 20230619155427.png]]

##### 1.提交然后抓包修改name的值
![[Pasted image 20230619155442.png]]

放包之后就可以看到如下页面了
![[Pasted image 20230619155458.png]]

对name的值那个修改（这里肯定会有人问为什么是name，因为message里面的东西是要存储的解析器并不会解析message里面的内容，而是要解析name里面的内容便于查询，类似于标识符的概念，用户进行后续操作时都是对name以name为唯一识别进行操作）//以上只是我个人对于这个问题的理解，只是便于理解（也不知道有没有写清楚，如果不懂的同学可以去查询其他的资料）


### 级别：Medium

这一级别与反射型xss的中级是一样的通过大小写或者双写，只不过是通过抓包修改数据包。

### 级别：High

这一级别与反射型xss的高级是一样的通过修改标签，只不过是通过抓包修改数据包。


