 
	精灵图（Sprite Sheet或CSS Sprites）其实就是将多个小的图像打包到一张大图中，然后通过CSS的`background-position`属性从中裁剪出需要的图像部分。这是一种用来提高网页性能的优化方法。

当一个网页需要加载多个小图像（例如图标、按钮等）时，每载入一个图像都会产生一次HTTP请求。这种请求的开销在图像数量较多时会变得非常明显。使用精灵图，我们可以将这些图像打包到一张大图中，只需要加载一次就可以使用所有的图像，从而减少HTTP请求的数量。