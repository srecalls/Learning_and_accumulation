

**共同点：**

- 改变行内元素的呈现方式，将**display置为inline-block**  

- 使元素**脱离普通文档流**，不再占据文档物理空间

- 覆盖非定位文档元素

**不同点：**

- abuselute与fixed的根元素不同，**abuselute的根元素可以设置**，**fixed根元素是浏览器**。

- 在**有滚动条的页面**中，absolute会跟着父元素进行移动，fixed固定在页面的具体位置。
