在javascript中几乎所有的方法都有valueOf方法，这是一个内置方法，用于返回对象的原始值
[[JS 中 valueOf() 方法的详解]]

当需要将对象转换成原始值时，javascript引擎会自动调用对象里的valueOf方法，关键在于重写这个方法，实现这个需求，同时这里用到了 == 号，涉及到了隐式转换

## 解法
```js
let a = {
	value: 1,
	valueOf: function() {
		return this.value++
	}
}
```