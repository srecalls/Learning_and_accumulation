实现大数相乘（非负大整数相乘）
```js
function multiplyBigNum(num1, num2) {
    if (isNaN(num1) || isNaN(num2)) return ""
    num1 = num1 + ""
    num2 = num2 + ""
    let len1 = num1.length
    let len2 = num2.length
    let pos = []

	// 25 * 8
	// i = 1
	// j = 0
    // j放在外面，先固定被乘数的一位，分别去乘乘数的每一位，更符合竖式演算法
    for (let j = len2 - 1; j >= 0; j--) {
        for (let i = len1 - 1; i >=0; i--) {
            // 两个个位数相乘，最多产生两位数，index1代表十位，index2代表个位
            let index1 = i + j
            let index2 = i + j + 1
            // 两个个位数乘积加上当前位置个位数已积累的数字，会产生进位，例如08 + 7 = 15,产生了进位1
            let mul = num1[i] * num2[j] + (pos[index2] || 0)
            // mul包含新计算的十位，加上原有的十位就是最新的十位
            pos[index1] = Math.floor(mul / 10) + (pos[index1] || 0)
            // mul的个位就是最新的个位
            pos[index2] = mul % 10
        }
    }
    // 去掉前置0
    let result = pos.join('').replace(/^0+/, '')
    return result - 0 || '0'
}
```
![[Pasted image 20230718211955.png]]

![[Pasted image 20230719014953.png]]


这是一个用 JavaScript 实现的计算两个大数相乘的函数 `multiplyBigNum(num1, num2)`。下面是每一步的详细解释：

```js
function multiplyBigNum(num1, num2) {
    // 如果输入的参数不是数字，则返回空字符串
    if (isNaN(num1) || isNaN(num2)) return ""
    // 将输入的数字转换成字符串形式
    num1 = num1 + ""
    num2 = num2 + ""
    // 获取输入数字的长度
    let len1 = num1.length
    let len2 = num2.length
    // 定义一个数组 pos，用来存放乘积的每一位
    let pos = []
```

这里先判断输入的参数是否为数字，如果不是则返回空字符串。然后将输入的数字转换成字符串形式，并获取它们的长度。最后定义一个数组 pos，用来存放乘积的每一位。

```js
    // 两个个位数相乘，最多产生两位数，index1代表十位，index2代表个位
    // i 和 j 分别表示被乘数和乘数的位数，j 放在外面，先固定被乘数的一位，
    // 分别去乘乘数的每一位，更符合竖式演算法
    // 25 * 8
    // i = 1
    // j = 0
    // index1 = 1
    // index2 = 2
    for (let j = len2 - 1; j >= 0; j--) {
        for (let i = len1 - 1; i >=0; i--) {
            let index1 = i + j
            let index2 = i + j + 1
```

这里采用竖式演算法，从被乘数的最后一位和乘数的最后一位开始相乘，一位一位累加。循环中的 `i` 和 `j` 分别表示被乘数和乘数的位数，`j` 放在外面，先固定被乘数的一位，分别去乘乘数的每一位，更符合竖式演算法。`index1` 代表乘积的十位，`index2` 代表乘积的个位。

```js
            // 两个个位数乘积加上当前位置个位数已积累的数字，会产生进位，例如08 + 7 = 15,产生了进位1
            let mul = num[i] * num2[j] + (pos[index2] || 0)
            // mul 包含新计算的十位，加上原有的十位就是最新的十位
            pos[index1] = Math.floor(mul / 10) + (pos[index1] || 0)
            // mul 的个位就是最新的个位
            pos[index2] = mul % 10
        }
    }
```

这里计算乘积时，先将被乘数和乘数的对应位相乘，再加上当前位置个位数已积累的数字。如果乘积的个位数大于等于 10，就需要进位。例如 08 + 7 = 15，产生了进位 1。所以用 `mul` 存储两个个位数的乘积加上当前位置个位数已积累的数字。然后将 `mul` 的十位数加上原有的十位数，存储到 `pos[index1]` 中，将 `mul` 的个位数存储到 `pos[index2]` 中。

```js
    // 去掉前置0
    let result = pos.join('').replace(/^0+/, '')
    // 如果乘积为 0，则返回字符串 '0'
    return result - 0 || '0'
}
```

最后将数组 `pos` 中的每一位拼接成字符串，并去掉前导零。如果乘积为 0，则返回字符串 '0'。注意，这里用 `- 0` 将字符串转换为数字，是为了去掉字符串开头的 0。