```js
window.number = 2
var obj = {
	number: 3,
	db1: (function() {
		this.number *= 4
		return function() {
			console.log(this)
			this.number *= 5
		}
	})() // 直接调用
}
var db1 = obj.db1
db1() // Window
obj.db1() //Object
console.log(obj.number) // 15
console.log(window.number) // 40
```

这道题目看清起来有点乱，但是实际上是考察this指向的:
- 执行db1()时，this指向全局作用域，所以window.number * 4 = 8，然后执行匿名函数，所以window.number * 5 = 40;
- 2.执行obj.db1();时，this指向obj对象，执行匿名函数，所以obj.numer * 5 = 15。

```js
window.number = 2
var obj = {
	number: 3,
	db1: (function() {
		this.number *= 4
		return function() {
			console.log(this)
			}
	})()
}
var db1 = obj.db1
db1() // Window
obj.db1() //Object
console.log(obj.number) // 3
console.log(window.number) // 8
```

这题的关键在于立即执行函数如果在里面，则需要调用后执行，如果在外面，是整个的，则直接调用