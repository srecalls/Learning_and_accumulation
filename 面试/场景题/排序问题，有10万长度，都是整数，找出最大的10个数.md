要找出最大的10个数，可以使用堆来实现。具体做法是，维护一个大小为10的小根堆，遍历整个数组，将每个数与堆顶元素比较，如果大于堆顶元素，则将堆顶元素弹出，将该数加入堆中。遍历完数组后，堆中剩余的元素即为最大的10个数。

这种方法的时间复杂度为O(nlogk)，其中n为数组长度，k为要找的最大数的个数，因为每次插入元素到堆中的时间复杂度为O(logk)，总共插入n个元素，所以总时间复杂度为O(nlogk)。

相比于其他排序算法，使用堆来找出最大的k个数的方法具有时间复杂度低、空间复杂度小的优点，特别适合处理大规模数据的场景。

需要注意的是，如果要找出最小的k个数，则需要使用大根堆来实现。