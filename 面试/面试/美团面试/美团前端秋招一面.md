[![头像](https://uploadfiles.nowcoder.com/images/20230410/405008065_1681102506252/428DC77A55ABDC76A4DA5B564782FAB5?x-oss-process=image%2Fresize%2Cw_72%2Ch_72%2Cm_mfit)](https://www.nowcoder.com/users/405008065)

[Musio](https://www.nowcoder.com/users/405008065)[![](https://static.nowcoder.com/fe/file/site/www-web/prod/1.0.273/imageAssets/6308303d42ab389ee025.png)](https://www.nowcoder.com/users/405008065)

08-29 10:55华东理工大学 计算机类

关注

# 美团平台技术部一面

美团平台技术部一面：  
自我介绍  
实习和实习项目  
项目遇到难点（因为实习项目中提到了小程序，他问了微信小程序原生和uniapp有哪些区别）  
Vue-router的hash和history模式  
跨域
[[（1）CORS]]
场景题：百度的搜索联想词如何按顺序返回，如搜索abc,会请求a,ab,abc，如何保证最后获得abc的结果  
手撕：合并两个有序数组  
```js
const twoArr = (arr1, arr2) => {
	return arr1.concat(arr2).sort()
}
```
Promise.all
[[手写Promise.all]]
```js
const p1 = new Promise(function(resolved, reject) => {
	setTimeout(() => {
		resolve(1)
	}, 1000)
})
const p2 = new Promise(function(resolved, reject) => {
	setTimeout(() => {
		resolve(2)
	}, 2000)
})
const p3 = new Promise(function(resolved, reject) => {
	setTimeout(() => {
		resolve(3)
	}, 3000)
})
const PromiseAll = (promises) => {
	return new Promise(function(resolved, reject) => {
		if (Array.isArray(promises)) {
			throw new Error("type error")
		}
		let resultArr = []
		for (let i = 0; i < promises.length; i++) {
			Promise.resolve(Promise[i]).then(res => {
				resultArr[i] = res
				if (i === promises.length) {
					return resolve(resultArr)
				}
			}, err => {
				return reject(error)
			})
		}
	})
}
```
10-100随机数 
```js
const randomNum = (min, max) => {
	return Math.floor(Math.random() * (max - min + 1) + min
}
console.log(randomNum(10, 100))
```
反问：因为看到我面的部门是美团平台技术部，感觉范围比较大，请面试官介绍一下部门  
学习前端的方法  
最后让我展示一下没有被问到的，但是有充分准备的东西，我写了个eventbus（因为前面提到小程序原生没有bus，手写过），还有vue源码中observer类和watcher类。