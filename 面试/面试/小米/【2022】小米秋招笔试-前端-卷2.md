  
## 1.关于 `<!DOCTYPE>` 下列说法错误的是

- [ ] A. `<!DOCTYPE>` 声明必须是 HTML 文档的第一行，位于 `<html>` 标签之前。
- [ ] B. `<!DOCTYPE>` 声明没有结束标签。
- [ ] C. `<!DOCTYPE>` 声明对大小写不敏感。
- [x] D.`<!DOCTYPE>` 声明是 HTML 标签，它是指示 web 浏览器关于页面使用哪个 HTML 版本进行编写的指令。

- `<!DOCTYPE>` 声明不是 HTML 标签；它是指示 web 浏览器关于页面使用哪个 HTML 版本进行编写的指令
- `<!DOCTYPE>` 声明没有结束标签。
- `<!DOCTYPE>` 声明对大小写不敏感。
下面是一个例子，展示了 `<!DOCTYPE>` 声明的使用：

```html
<!DOCTYPE html>
<html>
  <head>
    <title>Example Page</title>
  </head>
  <body>
    <h1>Hello, world!</h1>
    <p>This is an example page.</p>
  </body>
</html>
```

在这个例子中，`<!DOCTYPE html>` 声明告诉浏览器，这个页面使用 HTML5 进行编写。`<html>` 标签表示 HTML 文档的根元素，`<head>` 和 `<body>` 标签分别表示文档的头部和主体部分，`<title>` 标签定义了文档的标题。`<h1>` 和 `<p>` 标签用于创建页面的标题和段落。需要注意的是，`<!DOCTYPE>` 声明没有结束标签，同时对大小写不敏感，可以用大写或小写字母来表示。

## 4.下列关于Window Location 说法错误的是（ ）
- [x] A.location.host 返回 web 主机的域名
- [ ] B.location.pathname 返回当前页面的路径和文件名
- [ ] C.location.port 返回 web 主机的端口 （80 或 443）
- [ ] D.location.protocol 返回所使用的 web 协议（http: 或 https:）
![[【2022】小米秋招笔试-前端-卷2.png]]

host 属性是一个可读可写的字符串，可设置或返回当前 URL 的主机名称和**端口号**。
location.host 包含端口，比如是 127.0.0.1:81。如果端口是 80，那么就没有端口，就是 127.0.0.1。
location.hostname 不包含端口，比如是 127.0.0.1。


## 7.position定位，下列说法错误的是（ ）

- [ ] A.fixed元素，可定位相对于浏览器窗口的指定坐标，它始终是以 body 为依据
- [ ] B.absolute 的元素，如果它的父容器设置了 position 属性，并且 position 的属性值为 absolute 或者relative，那么就会依据父容器进行偏移
- [ ] C.fixed 属性的元素在标准流中不占位置
- [ ] D.relative元素以它原来的位置为基准偏移，在其移动后，原来的位置不再占据空间

position:relative，（没有脱离文档流）


## 8.请填写下面代码的输出结果（ ）
```js
var callbacks = []
for (var i = 0; i < 4; i++) {
	callbacks.push(
		function() {
			console.log(i);
		}
	)
}
callbacks.forEach(cb => cb());
```

- [ ] A.0, 1, 2, 3
- [ ] B.3, 3, 3, 3
- [x] C.4, 4, 4, 4
- [ ] D.0, 0, 0, 0

for循环里的函数没有直接调用，只是把4个一模一样的函数添加到数组里，再通过遍历调用每一个数组元素(函数),因为i=4跳出循环，所以console.log(i)全部打印4
如果换成let i 就会打印0123 但是题目是var i 会打印四个4 使用let会存在块级作用域 var不会


## 9.输出结果：
`const number = [1, 2, 3, 4, 5, 4, 3, 2, 1];`
`console.log(number.lastIndexOf(4)); （ ）`
[[JavaScript常用数组操作方法，包含ES6方法]]
- [ ] A.2
- [ ] B.3
- [ ] C.4
- [x] D.5


## 10.下列哪些不是浏览器支持的css选择器：（ ）

- [ ] A.div + p
- [ ] B.div:nth-child(1)
- [x] C.a:disabled
- [ ] D.input:disabled

a标签不支持disabled属性，
div + 


## 14.html5有哪些新特性？

- [x] A.Canvas
- [x] B.Web Storage
- [x] C.Web Workers
- [ ] D.Cookie

新特性：

	1.语义化标签     
	2.增强型表单包括属性以及元素    
	3.新增视频<video>和音频<audio>标签    
	4.Canvas 图形   
	5.地理定位    
	6.拖放API    
	7.SVG绘图    
	8.Web Worker    
	9.Web Storage    
	10.Web Socket 


	- 增加了语义化标签；  
	- 增加了重要的媒体标签<audio>和<video>，还引入了<source>标签配合媒体标签使用；  
	- 新增了<canvas>，使用 JavaScript 在就可以网页上绘制图像；  
	- 增加了draggable属性设置元素可拖放；  
	- 提供了两种在客户端存储数据的新方法localStorage和sessionStorage；  
	- 增加了DOM查询操作querySelector和querySelectorAll；  
	- 此外，还增加了大量的表单类型和表单属性；


## 15.js 中现在比较成熟的模块加载方案?
- [x] A.CommonJS
- [ ] B.EMD
- [x] C.AMD
- [x] D.CMD

js 中现在比较成熟的有四种模块加载方案。

1. 第一种是 CommonJS 方案  
2. 第二种是 AMD 方案  
3. 第三种是 CMD 方案  
4. 第四种方案是 ES6 提出的方案，使用 import 和 export 的形式来导入导出模块。


## 19.下列属于宏任务的是？

- [x] A.setTimeout
- [ ] B.MutationObserver
- [ ] C.Promise.then catch finally
- [x] D.requestAnimationFrame
- [x] E.setInterval


宏任务（Macrotask）大概如下：

	setTimeout 
	setInterval 
	MessageChannel I/O
	setImmediate（Node环境） 
	script（整体代码块）

微任务（Microtask）大概如下：

	MutationObserver（浏览器环境） 
	promise.[ then/catch/finally ]


## 21.【集合的所有子集】

	问题描述: 集合的所有子集 给定一组不含重复元素的整数数组 nums，返回该数组所有可能的子集。示例如下：
	输入样例1：nums = [1,2,3] 
	输出样例1：[[],[1],[2],[1,2],[3],[1,3],[2,3],[1,2,3]] 
	输入样例2：nums = [0] 
	输出样例2：[[],[0]]

[[剑指 Offer II 079. 所有子集]]


## 22.【字符串相乘】

问题描述: 

	给定两个以字符串形式表示的非负整数 num1 和 num2，返回 num1 和 num2 的乘积，它们的乘积也表示为字符串形式。 
	输入：字符串s 
	输出：字符串ans 
	输入样例1：num1 = "1", num2 = "4" 
	输出样例1： "4" 
	输入样例2：num1 = "123", num2 = "456" 
	输出样例2： "56088" 
	说明： 
	num1 和 num2 的长度小于110。 
	num1 和 num2 只包含数字 0-9。 
	num1 和 num2 均不以零开头，除非是数字 0 本身。 
	不能使用任何标准库的大数类型（比如 BigInteger）或直接将输入转换为整数来处理。

[[实现大数相乘（非负大整数相乘）]]