文件上传时转换为 MD5 值通常是为了验证文件的完整性和一致性，防止文件在上传过程中被篡改或损坏。MD5 是一种可靠的哈希算法，通过对文件内容进行哈希计算，可以得到一个唯一的、不可逆的字符串，可以用来验证文件是否被篡改。

而时间戳则只能保证文件上传的时间，无法验证文件的完整性和一致性。如果使用时间戳来验证文件，那么如果文件在上传过程中被篡改，时间戳也会发生变化，无法检测到文件的篡改。

因此，为了确保文件上传的安全性和完整性，一般会将文件转换为 MD5 值进行验证。当然，时间戳也可以用来记录文件上传的时间，但不能代替 MD5 值的作用。