this 是**执行上下文**中的**一个属性**，它指向**最后一次调用**这个方法的对象。在实际开发中，this 的指向可以通过四种调用模式来判断
[[03 JS的进阶知识点]]
![[Pasted image 20230514172714.png]]


## (1) 函数调用模式
- 第一种是**函数调用模式**，当一个函数不是一个对象的属性时，直接作为函数来调用时，this 指向全局对象。

```js
function foo() {
  console.log(this); // this 指向全局对象 window
}

foo(); // 函数作为一个普通函数来调用
```

## (2) 方法调用模式
- 第二种是**方法调用模式**，如果一个函数作为一个对象的方法来调用时，this 指向这个对象。
```js
var obj = {
  x: 10,
  foo: function() {
    console.log(this.x); // this 指向 obj 对象
  }
};

obj.foo(); // 调用 obj 对象的方法
```

## 构造器调用模式
- 第三种是**构造器调用模式**，如果一个函数用 new 调用时，函数执行前会新创建一个对象，this 指向这个新创建的对象。
```js
function Foo() {
  this.x = 10;
}

var obj = new Foo(); // 创建一个新对象并调用构造函数
console.log(obj.x); // 输出 10
```

## apply 、call 和 bind 调用模式
- 第四种是 **apply 、call 和 bind 调用模式**，这三个方法都可以显示的指定调用函数的 this 指向。

		- 其中 apply 方法接收两个参数: 一个是 this 绑定的对象.一个是参数数组。
		- call 方法接收的参数，第一个是 this 绑定的对象，后面的其余参数是传入函数执行的参数。也就是说，在使用 call() 方法时，传递给函数的参数必须逐个列举出来。
		- bind 方法通过传入一个对象，返回一个 this 绑定了传入对象的新函数。这个函数的 this 指向除了使用 new 时会被改变，其他情况下都不会改变。

```js
var obj1 = {
  x: 10
};

var obj2 = {
  x: 20
};

function foo(y) {
  console.log(this.x + y);
}

foo.call(obj1, 5); // 使用 call 方法指定 this 指向 obj1
foo.apply(obj2, [5]); // 使用 apply 方法指定 this 指向 obj2，传递参数数组
var newFoo = foo.bind(obj1); // 使用 bind 方法创建一个新函数并指定 this 指向 obj1
newFoo(5);
```

在这个例子中，通过 call 和 apply 方法可以明确地指定函数的执行上下文，从而改变 this 的指向。而 bind 方法则会返回一个新的函数，这个函数的 this 指向被绑定的对象，而不是原来函数的 this。

## 调用顺序
这四种方式，使用**构造器调用**模式的优先级最高，然后是 **apply、call 和 bind 调用**模式，然后是**方法调用**模式，然后是**函数调用**模式。

	new > apply、call、bind > 方法 > 函数

[[面试/代码输出/this/15. 代码输出结果]]
[[4.箭头函数与普通函数的区别]]
[[5.箭头函数的this指向哪里]]
