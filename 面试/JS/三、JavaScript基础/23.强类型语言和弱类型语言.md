强类型语言: **强类型语言**也称为**强类型定义语言**，是一种总是强制类型定义的语言，要求**变量**的使用要**严格符合定义**，所有变量都必须**先定义后使用**。Java和C++等语言都是强制类型定义的，也就是说，一旦一个变量被**指定了某个数据类型**，如果**不经过强制转换**，那么它就**永远**是这个数据类型了。例如你有一个整数，如果不显式地进行转换，你不能将其视为一个字符串。

弱类型语言: **弱类型语言**也称为**弱类型定义语言**，与强类型定义相反，JavaScript语言就属于弱类型语言。简单理解就是**一种变量类型可以被忽略的语言**。比如JavaScript是弱类型定义的，在JavaScript中就可以将字符串'12和整数3进行连接得到字符串'123'，在相加的时候会进行强制类型转换。

两者对比:强类型语言在速度上可能略逊色于弱类型语言，但是强类型语言带来的**严谨性**可以有效地帮助避免许多错误。


##### 例子
好的，下面我来举几个例子来说明强类型语言和弱类型语言的区别。

#### 强类型语言的例子

Java 是一种强类型语言，下面的代码演示了 Java 中对于变量类型的严格要求。在 Java 中，如果一个变量被指定了某个数据类型，那么它就永远是这个数据类型了，如果不经过强制转换，就不能将其视为其他类型。

```java
int num = 123; // 定义一个整数变量
String str = "hello"; // 定义一个字符串变量
// 下面的代码会提示编译错误，因为不能将整数和字符串进行相加
String result = num + str;
```

在上面的代码中，`num` 是一个整数类型变量，`str` 是一个字符串类型变量，它们的数据类型被严格指定。当我们试图将它们进行相加时，Java 编译器会提示错误，因为整数和字符串不能进行相加。

#### 弱类型语言的例子

JavaScript 是一种弱类型语言，下面的代码演示了 JavaScript 中变量类型可以被忽略的特点。在 JavaScript 中，同一个变量可以在不同的上下文中被赋予不同的类型，也可以将不同类型的变量进行相加，系统会自动进行类型转换。

```javascript
let num = 123; // 定义一个整数变量
let str = "hello"; // 定义一个字符串变量
// 下面的代码可以正常执行，将整数和字符串进行相加，系统会自动进行类型转换
let result = num + str;
console.log(result); // "123hello"
```

在上面的代码中，`num` 是一个整数型变量，`str` 是一个字符串型变量，它们的数据类型没有被严格定义。当我们将它们进行相加时，JavaScript 会自动将整数转换为字符串，然后将两个字符串连接起来，最终得到的结果是一个字符串类型的变量。

总之，强类型语言和弱类型语言的区别在于对于变量类型的严格要求。强类型语言要求变量的类型必须严格符合定义，所有变量都必须先定义后使用，而弱类型语言允许同一个变量在不同的上下文中被赋予不同的类型，也允许将不同类型的变量进行相加，系统会自动进行类型转换。强类型语言的严谨性可以有效地帮助避免许多错误，但是在开发效率上可能略逊色于弱类型语言。