

```js
class Parent extends React.Component {
	state = {
		parentMsg: ''
	}
	getChildMsg = (msg) => {
		console.log('接收到子组件数据', msg)
		this.setState({
			parentMsg: msg
		})
	}
	render() {
		return (
			<div>
				子组件: <Child getMsg={this.getChildMsg}
			</div>
		)
	}
}

class Child extends React.Component {
	state = {
		msg: '刷抖音'
	}
	handleClick = () => {
	//  自组件调用父组件传递过来的回调函数
		this.props.getMsg(this.state.msg)
	}
	render() {
		return (
			<div className='child'>
				子组件: <button onClick = {this.handleClick}>点我，给父组件传递数据</button>
			</div>
		)
	}
}
```
![[3.2子组件传递数据给父组件-2.png]]
第1.2步
![[3.2子组件传递数据给父组件.png]]



第3、4步
![[3.2子组件传递数据给父组件-1.png]]
![[3.2子组件传递数据给父组件-3.png]]



