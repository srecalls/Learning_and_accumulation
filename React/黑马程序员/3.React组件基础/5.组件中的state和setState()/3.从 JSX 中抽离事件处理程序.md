![[3.从 JSX 中抽离事件处理程序-1.png]]

JSX本身的功能是描述页面结构的，最好不要把逻辑代码放到JSX里面
```js
class App extends React.Component {
	state = {
		this.count = 0
	}
	render() {
		return (
			<button onClick={() => {
				setState({
					count: this.state.count + 1
				})
			}}>
			+1
			</button>
		)
	}
}
ReactDOM.render(<App/>, document.getElementById('root'))
```


如果直接抽出来
```js
class App extends React.Component {
	state = {
		this.count = 0
	}
	onIncrement() {
		console.log('事件处理程序中的this', this) // undefined
		this.setState({
			count: this.state.count + 1
		})
	}
	render() {
		return (
			<button onClick={/*() => {
				setState({
					count: this.state.count + 1
				})
			}*/}>
			+1
			</button>
		)
	}
}
ReactDOM.render(<App/>, document.getElementById('root'))

```

![[3.从 JSX 中抽离事件处理程序.png]]


因为this的问题