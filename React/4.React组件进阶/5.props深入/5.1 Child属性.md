
![[5.1 Child属性-2.png]]
## 文本节点
```js
const App = props => {
	console.log(props)
	return (
		<div>
			<h1>组件标签的子节点:</h1>
			{props.children}
		</div>
	)
}

ReactDOM.render(<App>我是子节点</App>, document.getElementById('root'))
```
![[5.1 Child属性.png]]

## 标签
```js
const App = props => {
	console.log(props)
	return (
		<div>
			<h1>组件标签的子节点:</h1>
			{props.children}
		</div>
	)
}

ReactDOM.render(
	<App>
		<p>我是子节点</p>
	</App>, document.getElementById('root')
)
```
![[5.1 Child属性-1.png]]

## 组件
```Js
const Test = () => return <button>+1</button>
ReactDOM.render(
	<App>
		<Test/>
	</App>, document.getElementById('root')
)
```

## 函数
```js
const App = props => {
	console.log(props)
	props.children()
	return (
		<div>
			<h1>组件标签的子节点:</h1>
			{/*props.children*/}
		</div>
	)
}

ReactDOM.render(
	<App>
		<p>() => console.log('这是一个子节点')</p>
	</App>, document.getElementById('root')
)
```

