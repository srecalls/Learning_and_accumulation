![[3.3兄弟组件之间的通讯.png]]


```js
class Counter extends React.Component {
	//. 提供共享状态
	state = {
		count: 0
	}
	handleClick = () => {
		this.setState({
			this.state.count + 1
		})
	}
	render() {
		return (
			<div>
				<Child1 count={this.state.count}>
				<Child2 handleClick={this.handleClick}>
			</div>
		)
	}
}

const Child1 = (props) => {
	return (
		<h1>计数器：{props.count}</h1>
	)
}

const Child2 = () => {
	return (
		<button onClick={this.props.handleClick()}> +1 </button>
	)
}
```
实现功能
点击Child2里按钮，使Child1里计数器显示增加

![[3.3兄弟组件之间的通讯-1.png]]