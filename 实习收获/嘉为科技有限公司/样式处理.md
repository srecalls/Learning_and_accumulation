```js
<span v-else-if="item.id === 'mom'" :class="[props.row['is_growth'] ? 'more' : 'less']">{{ (props.row['is_growth'] ? '+' : '-') + handleCost(props.row[item.id]) + '%' }}</span>
```

利用:class="[]"来进行样式的调整
```css
.more {
	color: #c72a29
}

.less {
	color: #65c5a3
}
```