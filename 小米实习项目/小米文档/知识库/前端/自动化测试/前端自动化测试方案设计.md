前端自动化测试方案设计

## 一、需求背景

众所周知的原因，前端作为一种特殊的 GUI 软件，做自动化测试困难重重。在快速迭代，UI 变动大的业务中，自动化测试想要落地更是难上加难，所以自动化测试在前端团队目前还是一个薄弱环节。

自动化测试能够预防无意引入的 bug，并鼓励开发者将应用分解为可测试、可维护的函数、模块、类和组件。这能够帮助你和你的团队更快速、自信地构建复杂的 Vue 应用。

所以为了加强前端项目的质量保障，守护业务迭代的质量和稳定性，规避代码上线前的风险，加强事前质量问题预防，将在前端重点项目中引入前端自动化测试流程。

## 二、项目成员

|   |   |
|---|---|
|**职责**|**人员**|
|产品||
|负责人||
|UI||
|服务端||
|客户端||
|测试||

## 三、项目排期

|   |   |   |   |   |
|---|---|---|---|---|
|**排期**|**目标**|**事项**|**开发**|**备注**|
||||||
||||||
||||||
||||||
||||||

## 四、方案设计

appstore-mobile是实现商店h5页面的c端项目库，单一页面日活可达到百万，同时还承担着商店重要的直投落地任务以及其它拉活任务等，保证页面的稳定运行十分重要。所以优先选择appsote-mobile项目落地前端自动化测试流程。

考虑到appstore-mobile项目是一个基于vue+typeScript架构的代码库，在设计测试策略时，应该利用以下几种测试类型：

- **单元测试**：检查给定函数、类或组合式函数的输入是否产生预期的输出或副作用。
- **组件测试**：检查你的组件是否正常挂载和渲染、是否可以与之互动，以及表现是否符合预期。这些测试比单元测试导入了更多的代码，更复杂，需要更多时间来执行。
- **端到端测试**：检查跨越多个页面的功能，并对生产构建的 Vue 应用进行实际的网络请求。这些测试通常涉及到建立一个数据库或其他后端。

### 单元测试

单元测试侧重于逻辑上的正确性，只关注应用整体功能的一小部分。

#### **方案选择**

- [Vitest](https://cn.vitest.dev/) 因为由 `create-vue` 创建的官方项目配置是基于 Vite的，所以我们推荐你使用一个可以利用同一套 Vite 配置和转换管道的单元测试框架。Vitest 正是一个针对此目标设计的单元测试框架，它由 Vue / Vite 团队成员开发和维护。在 Vite 的项目集成它会非常简单，而且速度非常快。
- [Peeky](https://peeky.dev/) 是另一速度极快的单元测试运行器，对 Vite 集成提供第一优先级支持。它也是由 Vue 核心团队成员创建的，并提供了一个基于图形用户界面（GUI）的测试界面。
- [Jest](https://jestjs.io/) 是一个广受欢迎的单元测试框架，专注于简易性的 JavaScript 测试框架。一个其独特的功能是可以为测试生成快照 (snapshot)，以提供另一种验证应用单元的方法。Vue CLI官方插件

Jest社区活跃度高，语法简单易懂，功能全面，报错信息良好，测试报告清晰，可查看覆盖率等，故而选取Jest作为单元测试方案

#### **测试模块**

##### 测试维度

- 接口功能性测试
    - 是否被顺利调用
    - 参数是否符合预期
- 局部数据结构测试

保证数据结构的正确性

- 变量是否有初始值或在某场景下是否有默认值
- 变量是否溢出

- 边界条件测试
    - 变量无赋值(null)
    - 变量是数值或字符
    - 主要边界：最大值，最小值，无穷大
    - 溢出边界：在边界外面取值+/-1
    - 临近边界：在边界值之内取值+/-1
    - 字符串的边界，引用 "变量字符"的边界
    - 字符串的设置，空字符串
    - 字符串的应用长度测试
    - 空白集合
    - 目标集合的类型和应用边界
    - 集合的次序
    - 变量是规律的，测试无穷大的极限，无穷小的极限
- 所有独立代码测试

保证每一句代码，所有分支都测试完成，主要包括代码覆盖率，异常处理通路测试

- 语句覆盖率：每个语句都执行到了
- 判定覆盖率：每个分支都执行到了
- 条件覆盖率：每个条件都返回布尔
- 路径覆盖率：每个路径都覆盖到了

- 异常模块测试

后续处理模块测试:是否包闭当前异常或者对异常形成消化,是否影响结果!

##### appstore-mobile重点模块梳理

|   |   |   |   |   |
|---|---|---|---|---|
|模块|单元模块|测试点|备注||
|composables/ButtonHandler|下载|- 参数拼接是否正确<br>- 是否正确调起客户端下载接口<br>- 打点上报是否正确调起，参数是否正确|||
|Loadpage||||
|Reserve||||
|composables/FormatAppList|FormatApp|- 调用方法返回值是否符合预期<br>- 参数异常处理测试|||
|FilterAppList||||
|FormatAppList||||
|composables/ScrollRequest|ScrollRequest|...|||
|composables/selectApp|UseSelectApp||||
|composables/VideoPlayer|UseVideo||||
|VideoManager||||
|composables/useTouch|-||||
|statistics打点相关|createReqQuery||||
|pageView||||
|installerrecord||||
|removeRepeatExposure||||
|setRecordParams||||
|trackRecord||||
|videoExposure||||
|TrackServices|ActionTrackService||||
|BaseTrackService||||
|ClickTrackService||||
|DownloadTrackService||||
|ExposeTrackService||||
|RequestTrackService||||
|SlideTrackService||||
|SubscribeTrackService||||
|Utils||||
|ViewTrackService||||
|webpack打包模块|-|校验打包输出文件是否正确|||

#### 测试用例

##### **测试用例****模版**

- 测试文件目录：tests/unit/***
- 测试文件命名：与测试模块文件名保持一致 例installBtn.ts -> installBtn.spec.js
- 测试用例编写按照如下方式

```
describe('【UT】安装按钮模块-开始下载:installApp', () => {
  test('正常下载, 打点上报', () => {
      // 断言
  });
  test('未成年模式禁止安装', () => {
      // 断言
  });
};
describe('【UT】安装按钮模块-打开应用:openApp', () => {})
```

##### **测试用例****规范**

- 可衡量：单测的编写应该是可以用具体的指标衡量的
    - 单测通过率要求100%，语句覆盖率：>=70%，分支覆盖率：100%，函数覆盖率：100%，行覆盖率要求50%
    - 老代码有逻辑变更时，单测也应该做相应的变更
    - 新业务提测前，必须保证老代码的单测通过率达到100%
- 独立性：单测应该是独立且相互隔离的
    - 一个单测只测试一个方法：保证单测的独立性
    - 单测不应该依赖于别的单测：独立性
    - 单测如果涉及到数据变更，必须进行回滚：保证单测的隔离性，不污染数据库数据
    - 单测应该测试目标方法本身的逻辑，对于被测试的方法内部调用的非私有方法应进行mock，推荐使用Mockito进行mock：独立性
- 规范性：单测的编写需要符合一定规范
    - 单测应该是无状态的：即单测应该可以重复执行，且无论跑几次都应该保证通过率。
    - 私有方法通过调用类的单测进行测试
    - 单测要覆盖到正常分支和异常分支。
    - 进行参数或属性校验。

校验包含正向和反向校验，即正确类型正确输出和异常类型返回异常信息等。

校验种类包含，参数个数、参数类型等。

- 一个测试类只对应一个被测类。
- 测试用例的变量和方法都要有明确的含义。
- 如果被测试的方法的逻辑体现在方法返回或成员变量中，则使用Assert断言验证该返回或成员变量
- 单测应在相应的目标方法开发完后立即编写，如能在开发前就开始编写则更好（TDD）

##### 编码原则-BCDE原则

B：Border，边界值测试，包括循环边界、特殊取值、特殊时间点、数据顺序等。

C：Correct，正确的输入，并得到预期的结果。

D：Design，与设计文档相结合，来编写单元测试。

E：Error，强制错误信息输入（如：非法数据、异常流程、非业务允许输入等），并得到预期的结果。

#### 测试部署

##### Jest测试基础插件

- babel-jest
- vue-jest
- @types/jest
- @vue/cli-plugin-unit-jest
- Jest

##### Jest配置-jest.config.js

```
module.exports = {
  // Automatically clear mock calls and instances between every test
  clearMocks: true,
  verbose: true,
  collectCoverage: true,
  preset: '@vue/cli-plugin-unit-jest/presets/typescript-and-babel',
  // A map from regular expressions to paths to transformers
  transform: {
    '^.+\\.vue$': 'vue-jest',
    '^.+\\.(ts|tsx|js|jsx)$': [
      'babel-jest',
      {
        presets: [
          ['@babel/preset-env', { targets: { node: true } }],
          ['@babel/preset-typescript'],
        ],
        plugins: ['@vue/babel-plugin-jsx'],
      },
    ],
  },
  transformIgnorePatterns: ['node_modules/(?!(@mi)/)'],
  testMatch: ['**/tests/**/*.spec.ts', '**/tests/**/*.spec.js'],
  moduleNameMapper: {
    '@/(.*)$': '<rootDir>/src/$1',
  },
};
```

##### 测试运行

Vue 通过脚手架创建，可直接执行yarn test:unit开始测试

### 组件测试

#### 方案选择

- vitest + @testing-library/vue 无头浏览器，浏览器事件不能使用

提示：Vitest 需要 Vite >=v3.0.0 和 Node >=v14

- Cypress：基于浏览器的测试方案
    - 热重启
    - 运行在浏览器环境中，支持浏览器中的api
    - 在浏览器中debug，实时查看数据

[Cypress](https://docs.cypress.io/guides/core-concepts/introduction-to-cypress) [Cypress入门](http://www.upcccz.cn/blog/index/test-about/cypress/note1/) [@vue/test-utils](https://v1.test-utils.vuejs.org/zh/api/options.html#context) [cypress-testing-library](https://testing-library.com/docs/cypress-testing-library/intro/)

**组件挂载库：**

- [@testing-library/vue](https://github.com/testing-library/vue-testing-library) 是一个 Vue 的测试库，专注于测试组件而不依赖其他实现细节。因其良好的设计使得代码重构也变得非常容易。它的指导原则是，测试代码越接近软件的使用方式，它们就越值得信赖。
- [@vue/test-utils](https://github.com/vuejs/test-utils) 是官方的底层组件测试库，用来提供给用户访问 Vue 特有的 API。@testing-library/vue 也是基于此库构建的。

模块测试方案选择的最重要的标准之一就是组件测试库应该尽可能与 Vue 生态系统兼容。

appstore-ui基于vite+vue搭建，因为会涉及到使用一些浏览器API，所以选择Cypress，且Cypress提供了很好的调试体验。

#### 测试模块

##### 测试维度：

- 组件渲染：是否正确渲染
- 组件交互：

模拟用户交互行为，是否正确处理：

- 弹窗弹了吗
- 组件隐藏正确吗
- 点击触发事件成功了吗
- 滚动加载正常吗
- ...

- 代码覆盖率，同上面单元测试
- 异常场景验证
    - 参数类型错误
    - 必传参数未传
    - slot传如不同类型值校验等

##### appstore-ui组件库

UI文档：https://appstore-ui.market.pt.xiaomi.com/docs/

文档中的组件都需要编写测试用例。

#### 测试用例

##### **测试用例****模版**

- 测试文件目录：每个compnent组件穿件一个__tests__文件夹，将测试文件放在该文件夹下
- 测试文件命名：src/**/__tests__/**/*.cy.js
- 测试用例编写按照如下方式：

```
describe('【CT】测试组件', () => {
  before(() => {
    // 所以用例开始前
  });
  after(() => {
    // 所有用例结束
  });

  context('测试模块一', () => {
    beforeEach(() => {
      // 挂载组件
      cy.mount(Component, {
          props: {},
          attrs: {
              class: ['test-demo'],
          },
          slots: {},
      })
    });

    it('测试点1', () => {
      // 断言
    });
    it('测试点二', () => {
      // 断言
    });
  });

  context('测试模块二', () => {
    // ...
  });
});
```

##### **测试用例****规范**

大部分规范都同单元测试一样，同时需要关注组件测试的一些特殊场景：

- 关注公开接口：触发的事件、props 和插槽等。

可以从以下角度设计测试用例：

- 对于 **视图** 的测试：根据输入 prop 和插槽断言渲染输出是否正确。
    
- 对于 **交互** 的测试：断言渲染的更新是否正确或触发的事件是否正确地响应了用户输入事件。
    

在编写测试用例时，多考察一下测试组件的健壮性，是否处理异常输入等。

- 不要去断言一个组件实例的私有状态或测试一个组件的私有方法。
    

测试实现细节会使测试代码太脆弱，因为当实现发生变化时，它们更有可能失败并需要更新重写。

如果一个方法需要测试，把它提取到一个独立的实用函数中，并为它写一个专门的单元测试。如果它不能被直截了当地抽离出来，那么对它的调用应该作为交互测试的一部分。

- 不要完全依赖快照测试。
    

断言 HTML 字符串并不能完全说明正确性。应当编写有意图的测试。

#### 

测试部署

##### 

Cypress测试插件

cypress/vite-dev-server

@cypress/vue

cypress

eslint-plugin-cypress

##### 配置

Cypress.config.js

```
/* eslint-disable @typescript-eslint/no-var-requires */
const { defineConfig } = require('cypress');

module.exports = defineConfig({
  component: {
    setupNodeEvents(on, config) {
      //
    },
    viewportWidth: 393,
    viewportHeight: 850,
    devServer: {
      framework: 'vue',
      bundler: 'vite',
    },
    specPattern: 'src/**/__tests__/**/*.cy.js',
  },
});
```

eslintrc.js

```
 extends: [
    "plugin:cypress/recommended"
  ],
```

##### 测试运行

```
cypress run --component --config-file cypress.config.js
cypress open  --component --config-file cypress.config.js --browser chrome
```

### 端到端测试

#### 方案选择

测试原则：**模拟真实用户场景以黑盒的形式完整检查应用是否正确**

- 真实：只操作真实看得见的元素
- 黑盒：不调用接口，不操作数据库
- 完整：以整体业务流程测试为目标

端到端测试更多的是站在用户角度去使用产品。

在方案上同样采用cypress方案

#### 测试页面

|   |   |   |   |
|---|---|---|---|
|页面|测试点|备注||
|activity|- 验证参数设置对页面渲染的影响，ref和oneTrackRef的设置是否符合预期<br>    - 不传任何参数<br>    - 只传identifier<br>    - 传identifier和refSuffix<br>- 点击安装按钮<br>    - 是否正常发起安装流程<br>- 点击横幅图片<br>    - 正确弹出二次确认下载弹窗<br>    - 点击弹窗-确认下载，正确发起下载流程|- 校验页面组件是否成功渲染<br>- 校验页面ref和oneTrackRef的设置是否符合预期<br><br>以上两条校验是所有页面都需要校验的基本条件||
|activity-ad<br><br>商业化广告活动||||
|software<br><br>软件页||||
|topic-page<br><br>话题页||||
|essential-firstV3<br><br>~<br><br>essential-firstV6<br><br>装机必备||||
|Favorite<br><br>我的收藏||||
|feed-app<br><br>标签应用页||||
|feedbackV1||||
|goldmi<br><br>金米奖||||
|newgame<br><br>新游||||
|personal-detail<br><br>个性化详情页||||
|redpack<br><br>红包活动||||
|cleanpage<br><br>手机清理页||||
|cat-list<br><br>分类列表||||
|double-list<br><br>双清单||||
|...||||

#### 测试用例

##### **测试用例****模版**

- 测试文件目录：tests/e2e/specs
- 测试文件命名：*.cy.ts
- 测试用例编写按照如下方式：

```
describe('【e2e】activity活动页测试', () => {
  before(() => {
    //
  });

  context('验证参数设置对页面渲染的影响', () => {
    it('访问页面: 不包含任何参数', () => {
        // 断言
    });
  });
  
  // ... 
});
```

##### **测试用例****规范**

- e2e测试不依赖任何架构
- e2e测试重点关注在页面是否正确渲染，不同参数对于页面渲染的影响，组件间交互逻辑是否正确等
    - 不要关注每个组件的细节，因为每个组件以及函数的测试已经在单元测试和组件测试中完成
- 将测试同学的测试用例一一实现

TODO:

- 只保留测试运行失败的视频
- 引入mocha测试报告
- 测试结果飞书通知
- cypress.on 监听，阻止无关紧要错误导致cypress进程退出

### 测试部署

集成到ci/cd流程中

TODO:添加流程图

## 五、上线方案

|   |   |   |   |   |
|---|---|---|---|---|
|**事项**|**操作时间**|**操作内容**|**操作人员**|**备注**|
|**准备工作**|xxx|配置CMS|||
|||||
|**上线步骤**|xxx|部署content|||
|||||
|**线上验证**|xxx|功能验证正常|||
|xxx|服务器状态正常|||
|xxx|日志正常|||
|xxx|接口失败率正常|||
|**回滚方案**|xxx|xbox回滚部署版本v1|||
|||||

## 六、方案评审

## 七、代码评审

服务端

客户端

## 八、补充总结

## 九、其他