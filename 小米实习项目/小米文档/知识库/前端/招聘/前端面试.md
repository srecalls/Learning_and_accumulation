

## 一面考察点以及题目

#### 考察点

1. javascript中高阶基础知识考察
2. 一些api的使用
3. 框架原理
4. 算法题，考察数据结构的设计

#### 题目

1. 如何通过原型实现继承？

```
function Person (name) {    
    this.name = name; 
}  
function Student () {
}  
Student.prototype = Person.prototype; 
Student.prototype.constructor = Student;  
let s = new Student('Tom'); 
console.log(s instanceof Person);
```

2. instanceof 和 new 分别在做什么？能否用代码来阐述？

```
function F() {}
function O() {}
var obj = new O();
O.prototype = new F();
console.log(obj instanceof O);
console.log(obj instanceof F);
console.log(obj.proto === O.prototype);
console.log(obj.proto.proto === F.prototype);

//
//false
//false
//false
//false
```

```
let p = new Person()
let p = (function () {    
    let obj = {};    
    obj.__proto__ = Person.prototype;
    // 其他赋值语句...        
    return obj; 
})();
```

3. ```
    while(x.__proto__!==null) {    
        if(x.__proto__===y.prototype) {
               return true;    
        }    
        x.__proto__ = x.__proto__.proto__; 
    } 
    if(x.__proto__==null) {return false;}
    ```
    
    关于作用域代码输出

```
for(var i = 0; i < 10; i++) {
    setTimeout(() => {
       console.log(i)
    }, 0)
}
```

```
var a = 1;
function b() {
   a = 10;
   return;
   function a() {}
}
b();
console.log(a);
```

```
var b = 10;
(function b() {  
    b = 20;
    console.log(b)
})()
```

```
(function A() {
    console.log(A);
    var A = 1;
    console.log(window.A);
    console.log(A);
}())
//对比下题
function A() {
    console.log(A);
    A = 1;
    console.log(window.A);
    console.log(A);
}
A();
```

4. 如何实现一个简单的Promise？
5. requestIdleCallback是什么，适用于什么场景，和requestAnimationFrame又有什么用处
6. jsbridge的原理
7. 如何计算白屏时间和首屏时间
8. 函数currying https://javascript.info/currying-partials
9. 数组扁平化处理：实现一个flatten方法，使得输入一个数组，该数组里面的元素也可以是数组，该方法会输出一个扁平化的数组。
10. 实现超出整数存储范围的两个大整数相加function add(a,b)。注意a和b以及函数的返回值都是字符串。
11. 运用你所掌握的数据结构，设计和实现一个 LRU (最近最少使用) 缓存机制 。
12. ```
    LRUCache(int capacity) 以正整数作为容量 capacity 初始化 LRU 缓存
    int get(int key) 如果关键字 key 存在于缓存中，则返回关键字的值，否则返回 -1 。
    void put(int key, int value) 如果关键字已经存在，则变更其数据值；如果关键字不存在，则插入该组「关键字-值」。当缓存容量达到上限时，它应该在写入新数据之前删除最久未使用的数据值，从而为新的数据值留出空间。
    ```
    
    移动端适配是怎么做的

响应式布局如何实现：

```
<div class="container">
  <div class="item"></div>
  <div class="item"></div>
  <div class="item"></div>
  <div class="item"></div>
  <div class="item"></div>
  <div class="item"></div>
</div>
```

## 二面考察点以及题目

#### 考察点

框架使用，react或者vue

项目中碰到的问题

项目优化流程

#### 题目

1. 如何使用 react hooks 实现 useFetch 请求数据

参考 [How to fetch data with React Hooks?](https://www.robinwieruch.de/react-hooks-fetch-data)

2. dom转化成图片

参考 https://www.zhangxinxu.com/study/201708/svg-foreignobject-dom-to-image.html

3. preload和prefetch的区别，以及如何使用

参考 https://juejin.cn/post/6844903562070196237

4. 长列表优化
5. 懒加载
6. 页面曝光打点
7. 用原生js实现页面内有一个input输入框，实现在数组arr查询命中词并要求autocomplete效果

## 三面考察点以及题目