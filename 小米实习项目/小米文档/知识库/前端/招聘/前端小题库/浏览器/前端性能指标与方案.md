#### 前端性能指标与方案

# 性能指标

## 🌐 从Network看性能指标

网络面板指标主要对应你对文件、缓存、服务器性能等等方面进行统计与加载；偏向于开发者端自测指标

#### 指标

我们先看一张图
![[前端性能指标与方案.png]]
###### **Queuing**

queuing为排队的意思，当一个网络请求发起时，由于不能马上执行，会进入一个排队的状态；主要原因有以下几种：

- 第一，页面中的资源是有[优先级](https://web.dev/fast/#prioritize-resources)的，例如CSS/字体文件/HTML/JS等等文件优先级最高；你可以打开network面板来查看；图片、视频、音频等等不是重要的资源，优先级会相对较低；两者相遇时，后者进入排队状态。

- 其次，浏览器会为每个域名最多维护 6 个 TCP 连接，如果发起一个 HTTP 请求时，这 6 个 TCP 连接都处于忙碌状态，那么这个请求就会处于排队状态。

- 然后，网络进程在为数据分配磁盘空间时，新的 HTTP 请求也需要短暂地等待磁盘分配结束。

###### Stalled

从TCP连接建立完成，到真正可以传输数据之间的时间差，TCP三次握手后，发送端发送数据后，一段时间内（不同的操作系统时间段不同）接收不到服务端ACK包，就会以 某一时间间隔(时间间隔一般为指数型增长)重新发送，从重传开始到接收端正确响应的时间就是stalled阶段。

###### Request sent

这个阶段一般很快，因为只需要把浏览器缓冲区的数据发送出去就结束了，并不需要判断服务器是否接收到了。

###### Waiting（TTFB）

这个阶段就是传说中的**“第一字节时间”，**TTFB 是反映服务端响应速度的重要指标，对服务器来说，TTFB 时间越短，就说明服务器响应越快；

一般监测这个指标就可以看到服务端的处理响应时间

###### Content Download

从第一字节时间到接收到全部响应数据所用的时间，你可以理解成从后端响应到内容数据全部加载到前端的时间。

###### 其他指标

其余完整的流程请参考[这里](https://developers.google.com/web/tools/chrome-devtools/network/reference?utm_source=devtools#timing-explanation)

- **DNS Lookup** 浏览器DNS解析时间
- **Initial connection** 其中包括TCP连接阶段、SSL 握手阶段
- **Proxy negotiation** 代理服务器代理协商阶段

#### 商店活动

[海外--三国俄罗斯黑五](https://globalnileapi.market.xiaomi.com/hd/luck-draw/index.html?refs=game&identifier=2d33310d96b7433ee7530efae9c20345&refSuffix=russiabf2020&n_backgroundColor=F3A137&a_backgroundColor=F3A137&a_windowBackgroundColor=F3A137&bodyColor=F3A137&s_darkMode=true&a_darkMode=true&show_dicon=false&show_sicon=false&activityType=default&title=%D0%9F%D0%BE%D0%BB%D1%83%D1%87%D0%B8%20Mi%20TV!&sid=19f0390ec43684eb46d5f4b7ea720198fdefault&rId=83828&ad=0&pos=carouselRecommend_0_2&posChain=carouselRecommend_0_2&newUser=false&activedTimeInterval=1729122820&adExchangeFlag=0&id=0&lo=RU&la=ru)

[国内--国庆画符活动](https://nileapi.market.xiaomi.com/hd/luck-draw/index.html?refs=&identifier=07003d65e8680dd05770d52b886e62b2&show_dicon=false&show_sicon=false&activityType=default&title=)

#### 优化

###### Queuing

- 排队时间过久，大概率是由浏览器为每个域名最多维护 6 个连接导致的。那么基于这个原因，可以使用**[域名分片技术](https://developer.mozilla.org/zh-CN/docs/Glossary/Domain_sharding)**，你就可以让 1 个站点下面的资源放在多个域名下面；

- 除了域名分片技术外，我个人还建议你把站点升级到 HTTP2，因为 HTTP2 已经没有每个域名最多维护 6 个 TCP 连接的限制了。

###### Stalled

stalled阶段是浏览器**得到要发出这个请求的指令，到请求可以发出的等待时间**，一般是代理协商、以及等待可复用的TCP连接释放的时间，不包括DNS查询、建立TCP连接等时间等。

> **单一服务发送时候stalled过长，往往是丢包所致，这也意味着网络或服务端有问题。**

> **多个服务并发导致stalled过长，是浏览器对同一个主机域名的并发连接数有限制，过长的请求是被阻塞了，处在队列中等待tcp连接**

优化可以采取以下方法：

- 将资源合理分布到多台主机上，可以提高并发数，但是增加并行下载数量也会增大开销，这取决于带宽和CPU速度，过多的并行下载会降低性能；
- 脚本置于页面底部；

###### DNS Lookup

请求某域名下的资源，浏览器需要先通过DNS解析器得到该域名服务器的IP地址。在DNS查找完成之前，浏览器不能从主机名那里下载到任何东西。

- 1、利用DNS缓存（设置`TTL(生存时间值)`【Time To Live】）；
- 2、利用Connection:keep-alive特性建立持久连接，可以在当前连接上进行多个请求，无需再进行域名解析；

###### waiting（TTFB）

第一字节时间（TTFB）时间过久这可能的原因有如下：

> **服务器生成页面数据的时间过久**。对于动态网页来说，服务器收到用户打开一个页面的请求时，首先要从数据库中读取该页面需要的数据，然后把这些数据传入到模板中，模板渲染后，再返回给用户。服务器在处理这个数据的过程中，可能某个环节会出问题。

> **网络的原因**。比如使用了低带宽的服务器，或者本来用的是电信的服务器，可联通的网络用户要来访问你的服务器，这样也会拖慢网速。

> **发送请求头时带上了多余的用户信息**。比如一些不必要的 Cookie 信息，服务器接收到这些 Cookie 信息之后可能需要对每一项都做处理，这样就加大了服务器的处理时长。

对于这三种问题，你要有针对性地出一些解决方案：

- 面对第一种服务器的问题，你可以想办法去提高服务器的处理速度，比如通过增加各种缓存的技术；
- 针对第二种网络问题，你可以使用 CDN 来缓存一些静态文件；
- 至于第三种，你在发送请求时就去尽可能地减少一些不必要的 Cookie 数据信息。

###### Content Download

如果单个请求的 Content Download 花费了大量时间，有可能是字节数太多的原因导致的。

- 通过条件Get请求，对比If-Modified-Since和Last-Modified时间，确定是否使用缓存中的组件，服务器会返回“304Not Modified”状态码，减小响应的大小；
- 移除重复脚本，精简和压缩代码，如借助自动化构建工具grunt、gulp等；
- 压缩响应内容，服务器端启用gzip压缩，可以减少下载时间；

## 💯 从LightHouse/Audits看性能指标

Audits/LightHouse基于谷歌web.dev开发者网站性能评测指标，偏向于用户侧的评测指标

#### 指标
![[前端性能指标与方案-1.png]]
###### FCP (First Contentful Paint)

由 Web 性能工作组在 W3C 标准 [Paint Timing](https://www.w3.org/TR/paint-timing/#sec-terminology) 中提出，在web.dev中定义与计算方法[first-contentful-paint](https://web.dev/first-contentful-paint/)

浏览器**首次绘制来自 DOM 的内容**的时间，内容必须是文本、图片（包含背景图）、非白色的 canvas 或 SVG，也包括带有正在加载中的 Web 字体的文本。

###### FMP (First Meaningful Paint) - 6.0 deprecated

由 Google 在 [Time to First Meaningful Paint: a layout-based approach](https://docs.google.com/document/d/1BR94tJdZLsin5poeet0XoTW60M0SjvOJQttKT-JK8HI/view#) 中提出，在web.dev中定义与计算方法[first-meaningful-paint](https://web.dev/first-meaningful-paint/)

从开始加载到浏览器**首次绘制像素**到屏幕上的时间，也就是页面在屏幕上首次发生视觉变化的时间。但此变化可能是简单的背景色更新或不引人注意的内容，它并不表示页面内容完整性，可能会报告没有任何可见的内容被绘制的时间。

在 Lighthouse 6.0 中已不推荐使用 FMP，建议使用 [Largest Contentful Paint](https://web.dev/largest-contentful-paint/) 代替。

###### LCP (Largest Contentful Paint)

由 Web 孵化器社区组（WICG）在 [Largest Contentful Paint API](https://wicg.github.io/largest-contentful-paint/#sec-intro) 中提出，是一个非标准化的 Web 性能度量。在web.dev中定义与计算方法[lighthouse-largest-contentful-paint](https://web.dev/lighthouse-largest-contentful-paint/)

**可视区域中最大的内容元素呈现**到屏幕上的时间，用以估算页面的主要内容对用户可见时间。

###### SI (Speed Index)

由 Google 在 [webpagetest.org](https://github.com/WPO-Foundation/webpagetest-docs/blob/master/user/Metrics/SpeedIndex.md) 中提出。web.dev中[speed-index](https://web.dev/speed-index/)

这是一个表示页面**可视区域中内容的填充速度**的指标，可以通过计算页面可见区域内容显示的平均时间来衡量。

###### FCI (First CPU Idle) - 6.0 deprecated

**页面第一次可以响应用户输入的时间**。

FCI 和 TTI 都是页面可以响应用户输入的时间。FCI 发生在**用户可以开始**与页面交互时；

TTI 发生在 **用户完全能够（可持续）** 与页面交互时。第一次可交互与可流畅交互的时间点如何确定可以在 Google 的 [First Interactive and Consistently Interactive](https://docs.google.com/document/d/1GGiI9-7KeY3TPqS3YT271upUVimo-XiL5mwWorDUD4c/edit#)中查阅。

在web.dev中[first-cpu-idle](https://web.dev/first-cpu-idle/)

在 Lighthouse 6.0 中，已不推荐使用 FCI

###### TBT (Total Blocking Time)

TBT度量阻止页面响应用户输入（例如鼠标单击，屏幕敲击或键盘按压）的总时间。

web.dev中[lighthouse-total-blocking-time](https://web.dev/lighthouse-total-blocking-time/)

###### TTI (Time to Interactive)

由 Web 孵化器社区组（WICG）在 [Time To Interactive](https://github.com/WICG/time-to-interactive) 中提出，是一个非标准化的性能度量指标。

**定义。**在web.dev中[interactive](https://web.dev/interactive/)

表示网页第一次 **完全达到可交互状态** 的时间点，浏览器已经可以持续性的响应用户的输入。完全达到可交互状态的时间点是在最后一个长任务（Long Task）完成的时间, 并且在随后的 5 秒内网络和主线程是空闲的。

从定义上来看，中文名称叫可持续交互时间或可流畅交互时间更合适。

> 长任务是需要 50 毫秒以上才能完成的任务

###### CLS (Cumulative Layout Shift)

CLS会度量在页面整个生命周期中发生的每个**意外布局偏移**的所有单独_布局转移分数_的总和。

通俗来讲就是[页面视觉稳定性](https://web.dev/user-centric-performance-metrics/#types-of-metrics)。web.dev中为[Cumulative Layout Shift](https://web.dev/cls/?utm_source=lighthouse&utm_medium=devtools)

---

完整指标请参考[这里](https://web.dev/lighthouse-performance/#metrics)、或者[这里](https://googlechrome.github.io/lighthouse/scorecalc/#FCP=0&SI=0&LCP=0&TTI=0&TBT=0&CLS=0&FMP=0&FCI=0&device=desktop&version=6&version=5)；更详细的性能指标标准请参考[Web性能工作组](https://www.w3.org/webperf/)。

#### 商店活动

[海外--三国俄罗斯黑五](https://globalnileapi.market.xiaomi.com/hd/luck-draw/index.html?refs=game&identifier=2d33310d96b7433ee7530efae9c20345&refSuffix=russiabf2020&n_backgroundColor=F3A137&a_backgroundColor=F3A137&a_windowBackgroundColor=F3A137&bodyColor=F3A137&s_darkMode=true&a_darkMode=true&show_dicon=false&show_sicon=false&activityType=default&title=Получи Mi TV!&sid=19f0390ec43684eb46d5f4b7ea720198fdefault&rId=83828&ad=0&pos=carouselRecommend_0_2&posChain=carouselRecommend_0_2&newUser=false&activedTimeInterval=1729122820&adExchangeFlag=0&id=0&lo=RU&la=ru)

[国内--国庆画符活动](https://nileapi.market.xiaomi.com/hd/luck-draw/index.html?refs=&identifier=07003d65e8680dd05770d52b886e62b2&show_dicon=false&show_sicon=false&activityType=default&title=)

#### 优化

详见[web.dev/lighthouse-performance](https://web.dev/lighthouse-performance/#metrics)

#### 🎯 核心指标
![[前端性能指标与方案-2.png]]
对于这些衡量用户体验的关键时间点，谷歌也给出了 4 种反映用户体验的指标，详细描述可以查看 [以用户为中心的性能指标](https://developers.google.com/web/fundamentals/performance/user-centric-performance-metrics)。

无法复制加载中的内容

## [🧮](https://emojipedia.org/abacus/) 各个指标计算方法

#### 性能度量

###### High Resolution Time **高精度时间**

该规范定义了一个 JavaScript 接口，该接口以毫秒级的分辨率提供当前时间，并且不受系统时钟偏差或调整的影响。

此标准有两个版本 Level 1 和 Level 2，Level 2 已经是正式发布的标准，所以 Level 1 已经过时了，官方不建议再使用，我们只需要知道 Level 2 定义了哪些规范就好。

- ~~[High Resolution Time Level 1](https://www.w3.org/TR/2012/REC-hr-time-20121217/#sec-high-resolution-time)~~ （已过时）
- [High Resolution Time Level 2](https://www.w3.org/TR/hr-time-2/) （REC）✔︎

Level 2 包含了这些内容：

- **定义了测量性能数据的初始时间（Time Origin）**

我们获取到的性能数据都是时间戳，需要一个初始时间来计算时间差，即某一阶段的耗时。

- **定义了高精度时间戳 DOMHighResTimeStamp**

用于存储以毫秒为单位的时间值，Web 性能规范定义的 API 获取到的时间都为高精度的时间戳。

- **定义了 Performance 对象，以及 Performance 对象的几个属性和方法。**
    - now()
    - timeOrigin
    - toJSON()

所有的性能数据都是通过 window.performance 返回的 Performance 对象获得的，下面的 Time 相关的性能标准所定义的属性和方法都是在 window.performance 和 Performance 对象中。

> Performance 对象最初是在 [Navigation Timing Level 1](http://www.w3.org/TR/navigation-timing/) 中定义的

###### Performance Timeline **性能时间线**

此标准对上述的 [HR-TIME-2](https://www.w3.org/TR/2016/CR-performance-timeline-2-20161208/#bib-HR-TIME-2) 规范中定义的 Performance 对象进行了扩展，**提供了基于特定筛选条件（Name、EntryType）检索性能度量的接口**，通过这些接口我们可以获得多种维度（页面、资源等）的性能度量时间线。

此标准目前有两个版本 Level 1 和 Level 2。Level 1 目前是 REC 状态，Level 2 规范还在草案阶段，当 Level 2 规范正式发布时， Level 1 也将被废弃。

- [Performance Timeline](https://www.w3.org/TR/2013/REC-performance-timeline-20131212/) （REC）
- [Performance Timeline Level 2](https://www.w3.org/TR/performance-timeline-2/) （WD） ✔︎

Level 2 规范包含了这些内容：

- **一、给** **[Performance](https://www.w3.org/TR/hr-time-2/#sec-performance)** **对象添加了三个方法：**
    - getEntries()
    - getEntriesByType()
    - getEntriesByName()

我们可以在浏览器控制台输入这段代码，看看 Entry 是什么。

> window.performance.getEntries();

返回的是一个数组，数组中包含这几种对象，每种对象都继承自 PerformanceEntry 对象，即都包含 PerformanceEntry 的两个字段： name 和 entryType。完整见**[附录2](https://xiaomi.f.mioffice.cn/docs/dock4jxyUHnSfqqaNPdqef3J2gf#1HfG7A)**

这些对象包含 Web 应用程序整个生命周期的各种性能数据度量。

> 这些对象会在其他 Time 相关的规范中定义，因此这些与 Time 相关的规范都会使用到 Performance Timeline 规范定义的 PerformanceEntry。

所以，我们可以根据 getEntriesByType() 和 getEntriesByName() 来获取特定的 name 和 特定的 entryType 的性能数据。例如：

> performance.getEntriesByType('paint');

> performance.getEntriesByName('https://www.baidu.com/xxx.png');
![[前端性能指标与方案-3.png]]

- **二、定义了** **[PerformanceEntry](https://www.w3.org/TR/performance-timeline/#dom-performanceentry)** **对象**

PerformanceEntry 对象具有 name、entryType、startTime、duration、 toJSON() 这些属性，是 PerformanceResourceTiming、PerformanceNavigationTiming 等对象的公共属性，所以将被后面介绍的其他规范定义的对象（如 PerformanceResourceTiming、PerformanceNavigationTiming 等）继承。

- **三、定义了** **[PerformanceObserver](https://www.w3.org/TR/performance-timeline/#dom-performanceobserver)** **对象**

用于观察性能时间线，以便在记录新的性能指标时发出通知, 这在采集性能数据时经常用到。例如下面的例子，观察 resource 类型的性能数据并打印。

###### Resource Timing **资源请求时序🌟**

该规范定义了用于访问文档中资源的完整计时信息的 API， 例如请求资源的 DNS、TCP、Request 等的开始时间和结束时间，可以帮助我们收集文档中静态资源的加载时间线、资源大小和资源类型。

此规范有两个版本 Level 1 和 Level 2， Level 1 在 2017 年就成为了候选推荐版本，到今天也没有正式发布。Level 2 当前还在工作草案阶段，在 2020.02.18 还在更新，但并没有在文档中说明与 Level 1 的关系。

- [Resource Timing Level 1](https://www.w3.org/TR/resource-timing-1/) （CR）
- [Resource Timing Level 2](https://www.w3.org/TR/resource-timing-2/) （WD）✔︎

从两个版本的内容看，内容差别不大，但 Level 2 新增了 [IANA Considerations](https://www.w3.org/TR/resource-timing-2/#sec-iana-considerations) 用于将 Timing-Allow-Origin 设置为临时消息头，并更新了资源时序处理模型。

![[前端性能指标与方案-4.png]]
例如我计算**Largest Contentful Paint（LCP）**指标时

```

const LCPData = [];
const observer = new PerformanceObserver(list => {
    const entries = entryList.getEntries();
    const entry = entries[entries.length - 1];
    let LCP = entry.renderTime - entry.loadTime;

    LCPData.push({LCP});
})
observer.observe({type: 'largest-contentful-paint'})

```

Level 2 规范包含了这些内容：

- **1、定义了** **[PerformanceResourceTiming](https://www.w3.org/TR/resource-timing-2/#dom-performanceresourcetiming)** **对象**

此对象描述了资源请求的性能时间线。

- **2、给 Performance 对象添加了如下方法**
    - clearResourceTimings()
    - setResourceTimingBufferSize()

- **3、定义了** **[Timing-Allow-Origin](https://www.w3.org/TR/resource-timing-2/#dfn-timing-allow-origin)** **响应头**

对于跨域请求的资源，获取到的 PerformanceResourceTiming 对象中的属性值（时间），由于[跨域限制](https://html.spec.whatwg.org/multipage/origin.html#same-origin)，浏览器不会将资源的性能数据提供给用户，这些时间值都会被设置为 0 。

如果服务端在请求资源的响应头中添加 Timing-Allow-Origin，则浏览器就会将此资源的性能时间值暴露给用户。

我们可以通过以下语句获取文档中所有资源的性能数据：

> performance.getEntriesByType('resource');

或者通过以下语句获取特定资源的性能数据：

> performance.getEntriesByName('path to img');

###### Navigation Timing **导航过程**中的**性能度量** **🌟**

此标准定义了**文档**导航过程中完整的性能度量，即一个文档从发起请求到加载完毕各阶段的性能耗时。

此标准目前有两个版本，Level 1 已经是 2012 年的版本了， Level 2 最新更新在 2020 年 1 月，将来 Level 2 会替代 Level 1 版本。

- [Navigation Timing Level 1](http://www.w3.org/TR/navigation-timing/) （REC）
- [Navigation Timing Level 2](https://www.w3.org/TR/navigation-timing-2/) （WD）✔︎

目前很多浏览器已经实现了 Level 2，建议使用 Level 2 规范定义的 API。因为 Level 1 和 Level 2 的差别比较大，但 Level 1 定义的 API 仍有很多人在用，所以下面都详细介绍下。

**Navigation Timing Level 1**

前面提到了 High Resolution Time API 定义了 Performance 对象，可通过 window.performance 获取。而 Navigation Timing Level 1 则是在此基础上增加了两个属性：timing 和 navigation。

规范包含了以下内容：

- **1、定义了 PerformanceTiming 对象**

用来衡量页面性能，我们可以通过通过 window.performance.timing 获取页面的性能数据，返回的对象中每个字段的含义可以在 [PerformanceTiming | MDN](https://developer.mozilla.org/zh-CN/docs/Web/API/PerformanceTiming) 上查阅。也可以查阅[该篇文章](https://javascript.ruanyifeng.com/bom/performance.html#toc0)。

按照事件发生的先后顺序，这些性能数据的 TimeLine 如下：
![[前端性能指标与方案-5.png]]
- **2、定义了 PerformanceNavigation 对象**

用来描述加载相关的操作，通过 window.performance.navigation 获得，返回的 PerformanceNavigation 对象存储了两个属性，它们表示触发页面加载的原因。这些原因可能是页面重定向、前进后退按钮或者普通的 URL 加载
![[前端性能指标与方案-6.png]]
返回的对象中每个字段的含义可以在 [PerformanceNavigation | MDN](https://developer.mozilla.org/zh-CN/docs/Web/API/PerformanceNavigation) 上查阅。

- **3、定义了 window.performance 属性**

为 Window 对象添加了 performance 属性：timing 和 navigation

**Navigation Timing Level 2**

2019 年 Web 性能工作组带来了 [Navigation Timing Level 2](https://www.w3.org/TR/navigation-timing-2/) ，将会替代 Navigation Timing Level 1。在 Navigation Timing Level 1 中定义的两个属性 performance.timing 和 performance.navigation 被废弃了。

Level 2 规范主要包含以下内容：

- **1、定义了** **[PerformanceNavigationTiming](https://www.w3.org/TR/navigation-timing-2/#sec-PerformanceNavigationTiming)** **对象**

此对象用于度量文档的性能，我们可以通过以下方式获取文档的性能数据，所有时间值都是以 [Origin Time](https://developer.mozilla.org/zh-CN/docs/Web/API/Performance/timeOrigin) 为起点测量的。

> window.performance.getEntriesByType("navigation");

这些属性并不都是 [PerformanceNavigationTiming | MDN](https://developer.mozilla.org/zh-CN/docs/Web/API/PerformanceNavigationTiming) 对象自有的，有一部分是从原型对象中继承的。

其中的 type 和 redirectCount 就是被废弃的 Navigation Timing Level 1 标准的 window.performance.navigation 返回的 [PerformanceNavigation](https://developer.mozilla.org/zh-CN/docs/Web/API/PerformanceNavigation) 对象所包含的内容。

而 Navigation Timing Level 1 中的通过 window.performance.timing 获取的 PerformanceTiming 对象所包含的描述文档性能的数据则是由以下对象共同描述的：

- [Navigation Timing Level 2](https://www.w3.org/TR/navigation-timing-2/) 定义的 PerformanceNavigationTiming 对象
- [Resource Timing Level 2](https://www.w3.org/TR/resource-timing-2/) 定义的 PerformanceResourceTiming 对象
- [Performance Timeline Level 2](https://www.w3.org/TR/performance-timeline-2/) 定义的 PerformanceEntry 对象

> 我们可以通过 [Resource Timing Level 2](https://www.w3.org/TR/resource-timing-2/) 标准中定义的 API window.performance.getEntriesByType("resource") 获取特定资源所需的时间长度。

由此，[Navigation Timing Level 2](https://www.w3.org/TR/navigation-timing-2/) 给出了新的时间线，与 Navigation Timing Level 1 不同的是将描述资源加载的时间用 PerformanceResourceTiming 对象封装了起来。
![[前端性能指标与方案-7.png]]
**2、定义了 NavigationType 的枚举值**

NavigationType 是一个枚举类型，包含四种值：navigate、reload 、 back_forward 和 prerender

好了，现在让我们来理解下 Level 2 标准中这些改变具体表现在哪里:

> 1、将对 Performance 对象的定义放在了 [PERFORMANCE-TIMELINE-2](https://www.w3.org/TR/navigation-timing-2/#bib-performance-timeline-2) 标准中

> 2、基于 [RESOURCE-TIMING-2](https://www.w3.org/TR/navigation-timing-2/#bib-resource-timing-2) 标准，从上文可以看到使用了在 RESOURCE-TIMING-2 中 定义的 PerformanceResourceTiming

> 3、使用了 [Performance Timeline Level 2](https://www.w3.org/TR/performance-timeline-2/) 定义的 PerformanceEntry 对象

> 4、所有的时间值都是以 [High Resolution Time Level 2](http://www.w3.org/TR/2013/WD-hr-time-2-20131203/) 中定义的 timeOrigin 为时间起点计算的。

> 5、[Level 1 中定义的导航类型](https://developer.mozilla.org/zh-CN/docs/Web/API/PerformanceNavigation/type) 有三种值：0， 1，2；对应了 [Level 2 中 NavigationType](https://www.w3.org/TR/navigation-timing-2/#sec-performance-navigation-types) 的 navigate、reload 和 back_forward。 但 Level 2 新增了 prerender 这种类型，而导航可被初始化为 prerender 类型是在 [RESOURCE-HINTS](https://www.w3.org/TR/navigation-timing-2/#bib-resource-hints) 标准中定义的。

> 6、PerformanceNavigationTiming 对象中的 redirectCount 字段

> 7、PerformanceResourceTiming 对象中的 nextHopProtocol 字段

> 8、PerformanceResourceTiming 对象中的 transferSize、encodedBodySize、decodedBodySize

> 9、secureConnectionStart 属性在 Level 1 中是可选的，但在 Level 2 中浏览器必需设置这个属性

###### Paint Timing **Paint 关键时间点**的**性能 🌟**

此规范定义了一个 API 用来记录在页面加载期间的一些关键时间点的性能度量，比如 First Paint 、First Contentful Paint。

此规范只有一个版本，这是第一个也是最新的版本，目前还在 Working Draft 阶段。

- [Paint Timing](https://www.w3.org/TR/paint-timing/) （WD）✔︎

此规范包含以下内容：

- **1、定义了 PerformancePaintTiming 对象**

用于描述在页面加载期间的一些关键时间点的性能度量，我们可以在控制台通过以下语句查看：

> performance.getEntriesByType('paint');

结果返回一个每一项都为 PerformancePaintTiming 类型的数组，一项为 first-paint ，另一项为 first-contentful-paint。

- **2、提出了一些关键时间点的定义，例如 First Paint 、First Contentful Paint**

First Paint，是从导航到浏览器将第一个像素呈现到屏幕的时间，这不包括默认的背景绘制，但包括非默认的背景绘制。这是开发人员关心页面加载的第一个关键时刻——当浏览器开始呈现页面时。

**First Contentful Paint** (FCP)，是当浏览器呈现来自 DOM 的第一位内容时，向用户提供页面实际加载的第一个反馈。这是用户第一次开始使用页面内容。

###### User Timing **让用户自定义时间节点来测量性能**

此规范定义了一个可以让 Web 开发者测量性能的 API 。目前有两个版本， Level 2 已正式发布，Level 3 还在草案阶段，将来也会替代 Level 2。

- [User Timing Level 2](https://www.w3.org/TR/user-timing-2/) （REC）
- [User Timing Level 3](https://www.w3.org/TR/user-timing-3/) (WD) ✔︎

Level 3 在 Level 2 的基础上做了改动，下面介绍 Level 3 规范包含的内容。

- **1、给** **[Performance](https://www.w3.org/TR/hr-time-2/#dfn-performance)** **对象添加了几个方法**
    - mark()
    - clearMarks()
    - measure()
    - clearMeasures()

使用方法如下，通过 mark() 方法可以在指定位置添加一个时间戳，记录执行到此位置时的时间。Mark 就是标记的意思，并且会给这个标记一个名称。

measure() 方法则可以测量两个时间点之间的间隔，并给它一个名称。

但是 Level 2 和 Level 3 中对 mark() 和 measure() 的用法是有区别的。

在 Level 2 中的用法如下，mark() 方法返回的 startTime 是执行到此标记语句时的时间。

> // Level 2 中的用法

> performance.mark("mySetTimeout-start");

> performance.measure( "mySetTimeout", "mySetTimeout-start", "mySetTimeout-end" );

在 Level 3 中，mark() 支持传入的第二个参数为一个对象，此对象的 startTime 字段可以支持用户自定义标记的开始时间，还提供了一个 detail 字段给用户描述其他信息；measure() 方法也接收第二个参数为一个对象，除了在 Level 2 中第二个参数和第三个参数表示的测量的开始时间和结束时间外，也提供了一个 detail 字段给用户描述其他信息。

> // Level 3 中的用法

> performance.mark("mySetTimeout-start", {

> detail: {component: 'component_name'},

> // 在 Level 2 中 startTime 不用传入，后台默认是 mark 标记的时间，Level 3 中可以支持用户自己定义了

> startTime: 123

> });

> performance.measure("click_to_update_component", {

> detail: {component: 'component_name'},

> start: startMark.startTime,

> end: performance.now(),

> });

- **2、定义了 PerformanceMark 对象**

描述 mark() 方法返回的数据, 可以通过 performance.getEntriesByType('mark') 获得，字段的具体含义可以在这里查看：[Performance.mark() | MDN](https://developer.mozilla.org/zh-CN/docs/Web/API/Performance/mark)， 其中 detail 字段是在 Level 3 规范中定义的。

- **3、定义了 PerformanceMeasure 对象**

描述 measure() 方法返回的数据，performance.getEntriesByType('measure')，字段的具体含义可以在[Performance.measure() | MDN](https://developer.mozilla.org/zh-CN/docs/Web/API/Performance/measure) 查看，其中 detail 字段是在 Level 3 规范中定义的。
![[前端性能指标与方案-8.png]]
###### Server Timing **服务端各节点性能**

我们知道前面已经介绍的 Resource Timing、Navigation Timing、Paint Timing 和 User Timing 规范描述的都是 Web 应用程序的性能特征，比如请求启动的时间、协商连接、接收响应等时间节点，这些性能度量浏览器都可以采集到，但请求是如何路由的、在服务器上花费的时间在哪里等无法获知。

此规范描述了如何将服务器端在请求-响应周期内的性能度量传递给用户代理，并定义了一个 API 使应用程序能够收集、处理和执行这些指标。

此规范只有一个版本，目前在 Working Draft 阶段。

- [Server Timing](https://www.w3.org/TR/server-timing/) （WD）✔︎

此规范包含以下内容：

- **1、定义了与服务端的通信协议：Server-Timing 响应头**

响应头信息如下, 可以在 www.w3.org/TR/server-t… 查看响应头的具体含义

> > GET /resource HTTP/1.1

> > Host: example.com

> < HTTP/1.1 200 OK

> < Server-Timing: miss, db;dur=53, app;dur=47.2

> < Server-Timing: customView, dc;desc=atl

> < Server-Timing: cache;desc="Cache Read";dur=23.2

> < Trailer: Server-Timing

> < (... snip response body ...)

> < Server-Timing: total;dur=123.4

TTFB

- **2、定义了描述服务端性能度量的接口 PerformanceServerTiming 对象**

浏览器会通过 [Server Timing Header 的解析算法](https://www.w3.org/TR/server-timing/#server-timing-header-parsing-algorithm) 将解析后的每一个性能度量用 PerformanceServerTiming 对象来表示。

- **3、给** **[PerformanceResourceTiming](https://www.w3.org/TR/server-timing/#extension-to-the-performanceresourcetiming-interface)** **对象添加了 serverTiming 属性**

每个 PerformanceServerTiming 描述服务端的一个性能度量信息，这些度量服务端性能的所有PerformanceServerTiming 对象放在一个数组中，挂在 PerformanceResourceTiming 对象的 serverTiming 属性上，我们可以通过以下语句获取：

###### Long Tasks API 检测长任务

当用户与页面交互时，应用程序和浏览器都会将浏览器随后执行的各种事件排队，例如，用户代理根据用户的活动安排输入事件，应用程序为 requestAnimationFrame 和其他回调等安排回调。一旦进入队列，这些事件就会被浏览器安排逐个出列并执行。

但是，有些任务可能需要很长时间，如果发生这种情况，UI 线程将被锁定，所有其他任务也将被阻止。对于用户来说，这是一个卡死的页面，浏览器无法响应用户的输入，这是目前 Web 上不良用户体验的主要来源。

此规范定义了一个 API，可以使用它来检测这些“长任务（Long Task）”的存在，“长任务”在很长一段时间内独占 UI 线程，并阻止执行其他关键任务，例如响应用户输入。

此规范目前只有一个版本，在 Working Draft 阶段。

- [Long Tasks API 1](https://www.w3.org/TR/longtasks-1/) （WD） ✔︎

此规范包含以下内容：

- **1、定义了 PerformanceLongTaskTiming 对象**

用于描述 Long Task 信息，对象中各字段的含义可在 [Long Tasks API | MDN](https://developer.mozilla.org/zh-CN/docs/Web/API/Long_Tasks_API) 查阅。

- **2、定义了什么是 Long Task**

Long Task 是指超过 50ms 的事件循环任务。

> 50 毫秒这个阈值标准来源于 [RAIL Model](https://developers.google.com/web/fundamentals/performance/rail) 中 [Response: process events in under 50ms](https://developers.google.com/web/fundamentals/performance/rail#response)(切换到英文版) 。

如何检测是否有 Long Task？我们可以使用 PerformanceObserver 这个 API。

> var observer = new PerformanceObserver(function(list) {

> var perfEntries = list.getEntries();

> for (var i = 0; i < perfEntries.length; i++) {

> // Process long task notifications:

> // report back for analytics and monitoring

> // ...

> }

> });

> // register observer for long task notifications

> observer.observe({entryTypes: ["longtask"]});

> // Long script execution after this will result in queueing

> // and receiving "longtask" entries in the observer.

![[前端性能指标与方案-9.png]]
#### API兼容性

![[前端性能指标与方案-10.png]]

当然也可以去Moailla上去查看以上几个的文档

例如：[Mozilla for PerformanceEventTiming](https://developer.mozilla.org/en-US/docs/Web/API/PerformanceEventTiming)

#### 两个工具类

计算资源加载时间、网络请求(ajax)监控等等方法监听资源时间 [Primus](https://github.com/pwstrick/Primus)

谷歌实验室监听LCP、FCP等等： [web-vitals](https://github.com/GoogleChrome/web-vitals)

## 附录

#### 附录1：各个浏览器TCP连接数

以下数据来源于Google

![[前端性能指标与方案-11.png]]

#### 附录2: `[PerformanceEntry.entryType](https://www.w3.org/TR/performance-timeline-2/#dom-performanceentry-entrytype)`

具体参见：[timing-entrytypes-registry](https://www.w3.org/TR/timing-entrytypes-registry/)

![[前端性能指标与方案-12.png]]
## TODO

1. 指标检测SDK
2. 指标检测流程方案
3. 之后针对各项优化方案与方向

## 参考链接及数据来源

[caniuse](https://caniuse.com/)

[NetWork reference](https://developers.google.com/web/tools/chrome-devtools/network/reference?utm_source=devtools#timing-explanation)

[web-vitals](https://github.com/GoogleChrome/web-vitals)

[web.dev](https://web.dev/learn/)

[chrome性能指标](https://googlechrome.github.io/lighthouse/scorecalc/#FCP=0&SI=0&LCP=0&TTI=0&TBT=0&CLS=0&FMP=0&FCI=0&device=desktop&version=6&version=5)

[chrome 各项指标权重](https://web.dev/performance-scoring/?utm_source=lighthouse&utm_medium=devtools)

[server-timing-api](https://web.dev/custom-metrics/?utm_source=devtools#server-timing-api)

[w3c/paint-timing](https://github.com/w3c/paint-timing)

[chrome.loadTimes (removed)](https://bugs.chromium.org/p/chromium/issues/detail?id=621512)

[document-ready-state](https://varvy.com/performance/document-ready-state.html)

[Web Performance Working Group](https://www.w3.org/webperf/)

[w3c特性搜索界面](https://www.w3.org/TR/)

[build-a-performance-culture](https://web.dev/fast/#build-a-performance-culture)

## [浏览器大纲](https://xiaomi.f.mioffice.cn/docs/dock41DyaSzy3RJbc1uAjS8HuUe)