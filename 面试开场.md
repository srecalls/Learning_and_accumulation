面试官你好，我是黄润，我毕业于广州的华南师范大学，学习的专业是网络工程专业。毕业之后校招进入了南京小米移动软件有限公司，目前在小米是做了两年的前端开发工程师。我在小米期间是做了 xx 个 大型项目。
一个是 xxx， 他是

一个是 xxx， 他是。

我个人的话是熟练掌握 react、javascript、html、css，在前端测试领域的话熟练 xxx，unit test 我们使用 jest来做的，可视化方面我们是擅长使用 echarts



你这个的日均pv是2000 +，就是你面向的用户是什么样子，是你们的内部用户吗

（考察面向的用户群体）

这个项目是为了解决什么问题呢
（了解对业务的熟悉流程）


项目是怎么实现做国际化的
国际化的方案是什么

多语言、多时区、多种计量单位
我们到没有做这块，我们主要做的还是英文的，项目不太涉及到时区的问题

问一下稳定性。
为了保证上线的稳定性，你们还会做哪一些动作

就是除了测试，我看测试还是做了很多东西的，包括单元测试，测试的落地效果是怎么样的
除了测试还有什么保证稳定性的

代码层面的话、会有eslint，动态监测一些语法错误，包括我们的Jest测试和端到端测试、覆盖率达到了90%，
用webpack来进行构建产物分析，比如webpack bundle analyzer，用来排除一些冗余的依赖，也做了一些 tree shanking和代码压缩。

在发布前会有一个预发布环境，现在预发布环境进行一个全链路的测试，模拟用户的真实场景

发布中如果有问题，我们也会有一些用户实事的响应，收到邮件，我们就可以及时的修改


发布是有灰度策略吗，

预发布覆盖的是哪些用户呢，它是按用户纬度去做的吗。

预发布的话只开放给我们这些开发人员


代码层面的测试和qa 的测试有没有交互层叠的地方。比如写单元测试，我理解工作量挺大的


测试是服务于QA的、还是服务于研发的


技术：
css
position 的定位一般包含哪些，以及他们分别是如何定位的

position： relative
当对自身在正常文档流中的一个定位，不脱离文档流，基于当前的位置进行相对定位，会占用盒子空间，

[[6. position的属性有哪些，区别是什么]]

position：absolute
基于父盒子进行定位，进行绝对定位，脱离文档流， 不占用盒子空间，元素的位置基于top，left，相对于static定位以外的一个父元素进行定位
[[绝对定位 absolute]]

position：sticky，正常时遵循文档流，如果滚动到特定的位置，指定的阈值，他会固定在一个目标位置，**最近的具有滚动机制的祖先元素**进行定位

position: static
默认属性，正常出现在文档流中，忽略top、bottom、z-index这些属性声明，块级元素从上到下纵向排布，行级元素从左到右横向排布

position：fixed
生成绝对定位的元素，相对于视口来进行绝对定位，脱离文档流，元素的位置在屏幕滚动时不会进行改变

position: inherit
继承父元素的定位属性

如果想设置浮动，怎么设置
[[7. display、float、position的关系]]
[[什么是浮动]]

display 比较常用的显示方式是什么

[[3. display的属性值及其作用]]
[[flex布局常见父项属性]]
[[flex布局子项常见属性]]

[[flex：1 完整写法]]

js语言他是一个单线程的模型，你同意这种说法吗，然后以及你是怎么理解的

我们要实现它的一个多线程，用的是事件循环，他是为了解决什么问题呢

比如我写了一个js脚本，里面有一个function，function里面有一个定时器，定时器有个callback和 定时，我执行这个js脚本，在过程中会发生哪些事件循环

谁去执行这个定时器的能力

你是如何理解原型和原型链的

每个构造函数中都有一个prototype属性，指向一个对象，这个对象包含了这个构造函数的所有实例对象共享的方法和属性，这个对象就被称之为原型对象， 原型的作用是节省存储空间，我每次new一个实例出来都需要 新划分一块区域存放 这个实例的属性和方法，

每个实例对象会有个proto属性指向这些原型，当我在实例对象中调用这些方法时就会 沿着沿着原型去 调用这些共享的属性和方法，原型对象也会有自己的原型，这个像链子一样层层往上 调用的关系 就叫做原型链

[[原型]]
[[对原型、原型链的理解]]

prototype 属于显示原型
—proto—属于隐式原型
他们的核心区别是什么
他们都指向的是这个原型对象

描述一下webpack的打包流程

比如我们执行这个 npm run build 执行这个scirpt到 输出一系列文件列表到本地
在这一系列过程中webpack做了哪些事情
[[7. webpack的构建流程]]

webpack是如何对这个代码进行拆分的
1. entry
2. split chunk pugins[[1.8.4 动态导入]]
3. es模块导入


react用的版本是多少
react 18

如何理解react hook的
跟类组件不一样，用hooks可以实现状态管理和模拟一些生命周期
生命周期useEffct、状态管理用useState

有自己封装过哪些hooks吗，比如过一些异步请求、api调用的这些，还有一些表单的hooks、基础的一些防抖、节流一些hooks


项目管理平台又遇到哪些比较困难的问题、怎么去定位和解决它的
举一到两个

首屏加载白屏的问题，先加载视口图内的一些节点，懒加载、后面再渲染一些节点，用户在拖拽的时候再加载一些其他的节点

数据量过大，用了一个虚拟列表

## 二面

项目解决了什么问题


项目做下来的收获是什么